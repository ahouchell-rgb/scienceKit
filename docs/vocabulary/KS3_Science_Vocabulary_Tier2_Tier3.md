# Springboard KS3 Science — Tier 2 & Tier 3 Vocabulary

*Extracted from the three Springboard KS3 Science Teacher Handbooks (Hodder, 2024). Grouped by subject → unit → sub-topic. Each word carries a short in-context gloss and, where useful, a `[root/affix = meaning]` morphology tag. Tier 2 = high-utility academic words (cross-subject); Tier 3 = subject-specific technical terms.*


---

# BIOLOGY

## Biology — Year 1

## B1 Cells

### B1.1 Microscopes
**Tier 2:** magnify — make an image look bigger `[root: magni- = great, large (L)]`; magnification — degree an image is enlarged `[root: magni- = great (L); -fication = process of making]`; focus — make an image sharp/clear `[root: focus = hearth, central point (L)]`; image — enlarged picture of an object `[root: imago = likeness (L)]`; object — the thing being viewed `[root: ob-ject = thrown before (L)]`; observe — look at carefully `[root: ob- = towards; servare = to watch (L)]`; adjust — change to bring into focus `[—]`; function — the job a part performs `[root: functio = performance (L)]`; demonstrate — show how to do something `[root: monstrare = to show (L)]`; structure — arrangement of parts `[root: struere = to build (L)]`; sample — small portion examined `[—]`; reflect — bounce light back/towards `[root: re- = back; flectere = to bend (L)]`.
**Tier 3:** microscope — instrument to view tiny things `[root: micro- = small; -scope = to look at (Gk)]`; cell — basic building block of life `[root: cella = small room (L)]`; lens — curved glass that bends light `[root: lens = lentil (L)]`; eyepiece — lens you look down `[—]`; objective lens — lower lens near the specimen `[root: ob-ject = thrown before (L)]`; stage — platform where the slide sits `[—]`; slide — glass strip holding the specimen `[—]`; cover slip — thin glass over the specimen `[—]`; specimen — sample viewed under microscope `[root: specere = to look (L)]`; iodine — orange stain for onion cells `[—]`; methylene blue — blue stain for cheek cells `[—]`; stain — dye to make cells visible `[—]`; mount — fix a specimen onto a slide `[—]`; visualise — make something able to be seen `[root: visus = sight (L)]`.

### B1.2 Cell structure
**Tier 2:** structure — arrangement of parts `[root: struere = to build (L)]`; function — the job a part does `[root: functio = performance (L)]`; survive — stay alive `[root: super- = beyond; vivere = to live (L)]`; substance — a type of matter/material `[root: sub-stare = to stand under (L)]`; suspend — held floating within `[root: sus-pendere = to hang (L)]`; component — a part of a whole `[root: com-ponere = to put together (L)]`; identify — recognise/name something `[root: idem = same (L)]`; instructions — coded directions to make proteins `[root: in-struere = to build in (L)]`; vary — differ, change `[root: varius = diverse (L)]`; collectively — taken together as a group `[root: col-legere = to gather (L)]`.
**Tier 3:** organism — a living thing `[root: organ = tool/instrument (Gk); -ism = state]`; unicellular — made of one cell `[root: uni- = one (L); cell = small room]`; multicellular — made of many cells `[root: multi- = many (L)]`; bacteria — single-celled microorganisms `[root: bakterion = little rod (Gk)]`; cell membrane — outer layer controlling entry/exit `[root: membrana = thin skin (L)]`; cytoplasm — gel where reactions happen `[root: cyto- = cell; -plasm = formed/moulded (Gk)]`; ribosome — site of protein synthesis `[root: -some = body (Gk)]`; genetic material — carries instructions to make proteins `[root: genesis = origin (Gk)]`; protein — molecule cells build and need `[root: proteios = primary (Gk)]`; synthesis — building/making of a substance `[root: syn- = together; thesis = placing (Gk)]`; sub-cellular — smaller than / inside the cell `[root: sub- = under/below (L)]`; carbon dioxide — waste gas removed by cells `[—]`; nucleus — control centre (introduced next topic) `[root: nucleus = kernel (L)]`.

### B1.3 Animal and plant cells
**Tier 2:** structure — arrangement of parts `[root: struere = to build (L)]`; function — the job a part does `[root: functio = performance (L)]`; identify — recognise the cell type `[root: idem = same (L)]`; control — direct the cell's activities `[—]`; process — a series of steps `[root: pro-cedere = to go forward (L)]`; release — let out (energy) `[—]`; rigid — stiff, not bending `[root: rigidus = stiff (L)]`; permanent — lasting, always present `[root: per-manere = to stay through (L)]`; three-dimensional — having length, width and depth `[root: di- = apart; metiri = to measure (L)]`; volume — amount of space occupied `[root: volumen = a roll (L)]`.
**Tier 3:** nucleus — controls the cell's activities `[root: nucleus = kernel (L)]`; DNA — genetic material carrying instructions `[—]`; mitochondrion — site of respiration `[root: mito- = thread; chondrion = granule (Gk)]`; respiration — releasing energy from food `[root: re-spirare = to breathe again (L)]`; cell wall — rigid outer layer of plant cells `[—]`; permanent vacuole — sap-filled sac keeping cells rigid `[root: vacuus = empty (L)]`; cell sap — fluid stored in the vacuole `[—]`; chloroplast — where photosynthesis happens `[root: chloro- = green; -plast = formed (Gk)]`; photosynthesis — making food using sunlight `[root: photo- = light; synthesis = putting together (Gk)]`; membrane — thin boundary layer `[root: membrana = skin (L)]`; cytoplasm — gel where reactions occur `[root: cyto- = cell; -plasm = formed (Gk)]`; ribosome — where proteins are made `[root: -some = body (Gk)]`.

### B1.4 Magnification
**Tier 2:** magnify — enlarge an image `[root: magni- = great (L)]`; magnification — how much bigger the image is `[root: magni- = great (L); -fication = making]`; calculate — work out a value `[root: calculus = pebble for counting (L)]`; equation — statement that two things are equal `[root: aequare = to make equal (L)]`; relationship — how values connect `[—]`; value — a number/quantity `[root: valere = to be worth (L)]`; convert — change from one unit to another `[root: con-vertere = to turn together (L)]`; measure — find the size of something `[root: mensura = a measuring (L)]`; estimate — a rough calculated value `[root: aestimare = to value (L)]`; multiply — increase by a factor `[root: multiplex = many-fold (L)]`; total — the whole/combined amount `[root: totus = whole (L)]`; actual — real, true size `[root: actus = a doing (L)]`; scenario — a described situation `[root: scena = scene (L)]`.
**Tier 3:** image size — size of the magnified picture `[—]`; actual size — real size of the object `[—]`; eyepiece lens — upper magnifying lens `[—]`; objective lens — lower magnifying lens `[root: ob-ject = thrown before (L)]`; total magnification — product of both lenses `[—]`; millimetre — one thousandth of a metre `[root: milli- = thousand (L); metron = measure (Gk)]`; centimetre — one hundredth of a metre `[root: centi- = hundred (L)]`.

### B1.5 Unicellular organisms
**Tier 2:** adapt — change to suit survival needs `[root: ad-aptare = to fit to (L)]`; specialised — changed for a particular job `[root: species = kind (L)]`; structure — arrangement of parts `[root: struere = to build (L)]`; function — the job a part does `[root: functio = performance (L)]`; survive — stay alive `[root: super-vivere = to outlive (L)]`; absorb — take in substances `[root: ab-sorbere = to suck in (L)]`; protect — keep from harm `[root: pro-tegere = to cover in front (L)]`; component — a part needed `[root: com-ponere = to put together (L)]`; relative — compared to something else `[root: re-latus = carried back (L)]`; scale — relative size/proportion `[root: scala = ladder (L)]`; variation — differences between things `[root: varius = diverse (L)]`; obtain — get/acquire `[root: ob-tenere = to hold onto (L)]`.
**Tier 3:** unicellular — made of one cell `[root: uni- = one (L)]`; bacterium (pl. bacteria) — single-celled microorganism `[root: bakterion = little rod (Gk)]`; microbe — a microscopic organism `[root: micro- = small; bios = life (Gk)]`; flagellum (pl. flagella) — tail-like structure for movement `[root: flagellum = whip (L)]`; slime capsule — protective outer coat `[root: capsula = little box (L)]`; cell wall — rigid outer layer `[—]`; nucleus — control centre (absent in bacteria) `[root: nucleus = kernel (L)]`; mitochondrion — respiration site (absent in bacteria) `[root: mito- = thread; chondrion = granule (Gk)]`; yeast — a unicellular fungus `[—]`; fungus — organism group including yeast `[root: fungus = mushroom (L)]`; protozoa — single-celled animal-like organisms `[root: proto- = first; zoon = animal (Gk)]`; nutrient — nourishing substance `[root: nutrire = to nourish (L)]`; species — a distinct kind of organism `[root: species = kind (L)]`.

### B1.6 Diffusion
**Tier 2:** transport — move substances from place to place `[root: trans- = across; portare = to carry (L)]`; transfer — move from one place to another `[root: trans- = across; ferre = to carry (L)]`; factor — a thing that affects a result `[root: facere = to do/make (L)]`; rate — how fast something happens `[root: rata = fixed amount (L)]`; increase — make greater `[root: in-crescere = to grow (L)]`; process — a series of steps `[root: pro-cedere = to go forward (L)]`; substance — a type of matter `[root: sub-stare = to stand under (L)]`; energy — capacity to do work/cause change `[root: en- = in; ergon = work (Gk)]`; temperature — how hot something is `[root: temperare = to mix (L)]`; direction — the way something moves `[root: di-rigere = to guide (L)]`; equal — the same in amount `[root: aequalis = even (L)]`; affect — have an influence on `[root: af-ficere = to act on (L)]`.
**Tier 3:** diffusion — movement from high to low concentration `[root: dif-fundere = to pour apart (L)]`; concentration — how packed particles are `[root: con- = together; centrum = centre (L)]`; particle — a tiny piece of matter `[root: particula = little part (L)]`; concentration gradient — change in concentration across space `[root: gradus = step (L)]`; surface area — total area of an outer surface `[—]`; cell membrane — layer particles diffuse through `[root: membrana = skin (L)]`; channel — tiny opening particles pass through `[—]`; carbon dioxide — waste gas that diffuses out `[—]`; glucose — a sugar/food molecule `[root: gleukos = sweet (Gk)]`; oxygen — gas that diffuses into cells `[root: oxy- = sharp/acid; -gen = producing (Gk)]`.

### B1.7 Specialised cells
**Tier 2:** specialised — adapted for a particular job `[root: species = kind (L)]`; adaptation — a feature that suits a function `[root: ad-aptare = to fit to (L)]`; adapt — change to suit a purpose `[root: ad-aptare = to fit to (L)]`; function — the job a cell does `[root: functio = performance (L)]`; structure — arrangement of parts `[root: struere = to build (L)]`; characteristic — a distinctive feature `[root: kharakter = stamp (Gk)]`; contract — squeeze/shorten `[root: con-trahere = to draw together (L)]`; increase — make greater `[root: in-crescere = to grow (L)]`; absorb — take in `[root: ab-sorbere = to suck in (L)]`; efficient — working with little waste `[root: ef-ficere = to accomplish (L)]`; comparative — using comparison language `[root: com-par = equal with (L)]`; extension — a part that sticks out `[root: ex-tendere = to stretch out (L)]`.
**Tier 3:** red blood cell — cell that carries oxygen `[—]`; biconcave — curved inward on both sides `[root: bi- = two; concavus = hollow (L)]`; haemoglobin — pigment that carries oxygen `[root: haemo- = blood (Gk); globin = globular protein]`; blood vessel — tube carrying blood `[—]`; nucleus — control centre (absent in RBC) `[root: nucleus = kernel (L)]`; mitochondria — respiration sites (many in muscle) `[root: mito- = thread; chondrion = granule (Gk)]`; muscle cell — cell that contracts to move things `[root: musculus = little mouse (L)]`; respiration — releasing energy from food `[root: re-spirare = to breathe again (L)]`; root hair cell — plant cell absorbing water and minerals `[—]`; mineral — inorganic nutrient from soil `[root: minera = ore (L)]`; palisade cell — leaf cell packed for photosynthesis `[root: palus = stake (L)]`; chloroplast — where photosynthesis happens `[root: chloro- = green; -plast = formed (Gk)]`; photosynthesis — making food from sunlight `[root: photo- = light; synthesis = putting together (Gk)]`; diffusion — high-to-low concentration movement `[root: dif-fundere = to pour apart (L)]`; surface area — outer area available for diffusion `[—]`.

## B2 Skeletal and muscular systems and organisation

### B2.1 The skeleton
**Tier 2:** function — the job something does `[root: functio = performance (L)]`; support — hold up/keep upright `[root: sup-portare = to carry under (L)]`; protection — keeping something from harm `[root: pro-tegere = to cover (L)]`; structure — arrangement of parts `[root: struere = to build (L)]`; property — a characteristic quality `[root: proprius = one's own (L)]`; stable — steady, not easily moved `[root: stabilis = firm (L)]`; connect — join together `[root: con-nectere = to bind together (L)]`; flexible — able to bend `[root: flectere = to bend (L)]`; system — parts working together as a whole `[root: systema = organised whole (Gk)]`; replace — put a new one in place of `[—]`; lubrication — reducing friction with a fluid `[root: lubricare = to make slippery (L)]`; force — a push or pull `[root: fortis = strong (L)]`.
**Tier 3:** skeleton — the framework of bones `[root: skeletos = dried up (Gk)]`; skeletal system — bones working together `[root: skeletos = dried (Gk)]`; bone — hard tissue of the skeleton `[—]`; joint — where two bones meet `[root: jungere = to join (L)]`; calcium — mineral that makes bones hard `[—]`; bone marrow — tissue that makes blood cells `[—]`; spine — backbone giving support `[root: spina = thorn (L)]`; skull — bone protecting the brain `[—]`; cartilage — flexible tissue protecting bone ends `[root: cartilago = gristle (L)]`; friction — force resisting rubbing surfaces `[root: fricare = to rub (L)]`; synovial fluid — natural lubricant in joints `[root: syn- = with; ovum = egg (L)]`; ligament — tough band joining bones `[root: ligare = to bind (L)]`; tendon — connects muscle to bone `[root: tendere = to stretch (L)]`; blood cells — cells made in bone marrow `[—]`.

### B2.2 Biomechanics
**Tier 2:** function — the job something does `[root: functio = performance (L)]`; contract — squeeze/shorten `[root: con-trahere = to draw together (L)]`; relax — lengthen/stop pulling `[root: re-laxare = to loosen (L)]`; transfer — move energy from one place to another `[root: trans-ferre = to carry across (L)]`; energy — capacity to cause change `[root: en-ergon = in-work (Gk)]`; conscious — done with awareness/control `[root: con-scire = to know with (L)]`; efficient — working with little waste `[root: ef-ficere = to accomplish (L)]`; opposite — facing the other way `[root: op-ponere = to place against (L)]`; measure — find a value `[root: mensura = measuring (L)]`; compare — look for similarities/differences `[root: com-par = equal with (L)]`; trend — a pattern in data `[—]`; relationship — how two variables connect `[—]`; variable — a factor that can change `[root: variare = to change (L)]`; scale — spaced values on an axis `[root: scala = ladder (L)]`; horizontal — going across, level `[root: horizon = boundary (Gk)]`; vertical — going straight up `[root: vertex = highest point (L)]`; origin — the zero point of a graph `[root: oriri = to rise (L)]`.
**Tier 3:** muscle — tissue that contracts to move `[root: musculus = little mouse (L)]`; skeletal muscle — muscle attached to bones `[root: skeletos = dried (Gk)]`; cardiac muscle — heart muscle, never tires `[root: kardia = heart (Gk)]`; smooth muscle — muscle lining hollow organs `[—]`; antagonistic — muscles working in opposition `[root: ant- = against; agon = contest (Gk)]`; biceps — front upper-arm muscle `[root: bi- = two; ceps = head (L)]`; triceps — back upper-arm muscle `[root: tri- = three; ceps = head (L)]`; tendon — connects muscle to bone `[root: tendere = to stretch (L)]`; respiration — energy release for contraction `[root: re-spirare = to breathe again (L)]`; dynamometer — device measuring grip strength `[root: dynamis = power; metron = measure (Gk)]`; newton — unit of force `[—]`; line of best fit — line drawn nearest all points `[—]`; x-axis — horizontal axis of a graph `[—]`; y-axis — vertical axis of a graph `[—]`; linear — forming a straight line `[root: linea = line (L)]`.

### B2.3 Principles of organisation
**Tier 2:** organisation — arrangement into a structured whole `[root: organon = tool (Gk); -isation = process]`; function — the job something performs `[root: functio = performance (L)]`; specialised — adapted for a particular job `[root: species = kind (L)]`; adaptation — a feature suiting a function `[root: ad-aptare = to fit to (L)]`; structure — arrangement of parts `[root: struere = to build (L)]`; system — parts working together `[root: systema = organised whole (Gk)]`; control — regulate/direct `[—]`; communicate — send signals/information `[root: communicare = to share (L)]`; component — a part of a whole `[root: com-ponere = to put together (L)]`; combine — join together `[root: com-binare = to pair together (L)]`; supply — provide what is needed `[root: sup-plere = to fill up (L)]`; force — a push or pull `[root: fortis = strong (L)]`; harmony — working smoothly together `[root: harmonia = joining (Gk)]`; connective — joining/holding together `[root: con-nectere = to bind (L)]`.
**Tier 3:** nerve cell — cell carrying electrical signals `[root: nervus = sinew (L)]`; dendrite — branch receiving signals `[root: dendron = tree (Gk)]`; axon — long tube carrying the signal `[root: axon = axis (Gk)]`; nerve ending — terminal that passes on signals `[—]`; nerve tissue — nerve cells working together `[—]`; tissue — group of the same cells working together `[root: texere = to weave (L)]`; muscle tissue — muscle cells working together `[root: musculus = little mouse (L)]`; cardiac muscle tissue — heart muscle tissue `[root: kardia = heart (Gk)]`; connective tissue — tissue holding organs together `[root: con-nectere = to bind (L)]`; organ — group of tissues with one function `[root: organon = tool (Gk)]`; organ system — organs doing one function `[root: organon = tool (Gk)]`; organism — a whole living thing `[root: organon = tool (Gk); -ism = state]`; capillaries — tiny blood vessels `[root: capillus = hair (L)]`; muscular-skeletal system — muscles and bones together `[root: musculus = little mouse (L); skeletos = dried (Gk)]`; respiratory — relating to breathing `[root: re-spirare = to breathe (L)]`; digestive — relating to breaking down food `[root: digerere = to separate (L)]`; reproductive — relating to producing offspring `[root: re- = again; producere = to bring forth (L)]`.

## Biology — Year 2

## B3 Nutrition and digestion

### B3.1 Healthy diet, energy requirements and dietary imbalance
**Tier 2:** component — a part making up a whole `[com- = together (L)]`; nutrient — substance the body needs `[nutri- = to nourish (L)]`; require / requirement — amount needed `[re- = again + quaerere = seek (L)]`; balanced / balance — right proportions of parts `[bi-lanx = two scales (L)]`; function / functioning — the job something does `[fungi = to perform (L)]`; insulate — to keep warmth in `[insula = island (L)]`; deficiency — a harmful shortage `[de-facere = to undo (L)]`; consume — to take in / eat `[con-sumere = use up (L)]`; symptom — a sign of a disease `[sym- = together + piptein = befall (Gk)]`; represent — to stand for on a chart `[re- = again + praesentare = show (L)]`; category — a labelled group `[kategoria = statement (Gk)]`; compare — to judge against each other `[com-par = equal (L)]`; interval — equal gap on a scale `[inter- = between (L)]`.
**Tier 3:** carbohydrate — energy-giving food group `[carbo- = coal/carbon + hydro- = water (Gk)]`; protein — food group for growth/repair `[protos = first (Gk)]`; vitamin — micronutrient for body processes `[vita = life (L)]`; mineral — inorganic dietary nutrient `[minera = ore (L)]`; fibre — indigestible plant bulk `[fibra = thread (L)]`; calcium — mineral for strong bones `[calx = lime (L)]`; iron — mineral in red blood cells `[—]`; anaemia — iron-deficiency blood disease `[an- = without + haima = blood (Gk)]`; obesity — excess body-fat condition `[obesus = fat (L)]`; diabetes — high-blood-sugar disease `[dia-bainein = pass through (Gk)]`; artery — vessel carrying blood from heart `[arteria (Gk)]`; kilojoule — unit of dietary energy `[kilo- = thousand (Gk)]`; ribosome — sub-cellular protein-maker `[soma = body (Gk)]`.

### B3.2 Digestive organs
**Tier 2:** digest / digestion — breaking food down `[di-gerere = carry apart (L)]`; absorb / absorption — to take in nutrients `[ab-sorbere = suck in (L)]`; organ — body part with a function `[organon = tool (Gk)]`; system — organs working together `[syn-histanai = set up together (Gk)]`; soluble / insoluble — able/unable to dissolve `[solvere = loosen (L)]`; dissolve — mix fully into liquid `[dis-solvere = loosen apart (L)]`; efficient / efficiency — working with little waste `[ef-ficere = accomplish (L)]`; surface area — total exposed area `[sur-face = above the face (L)]`; adapt / adapted — suited to a function `[ad-aptare = fit to (L)]`; membrane — thin covering layer `[membrana = skin (L)]`; projection — a part sticking out `[pro-jacere = throw forward (L)]`; magnified — enlarged in view `[magnus = large (L)]`.
**Tier 3:** enzyme — protein speeding chemical reactions `[en- = in + zyme = leaven (Gk)]`; diffusion / diffuse — spread from high to low concentration `[dif-fundere = pour apart (L)]`; oesophagus — food tube to stomach `[oiso- = carry + phagein = eat (Gk)]`; stomach — churning digestive organ `[stomachos = gullet (Gk)]`; intestine — gut tube for digestion/absorption `[intus = within (L)]`; pancreas — enzyme-releasing gland `[pan- = all + kreas = flesh (Gk)]`; rectum — waste-storing gut end `[rectus = straight (L)]`; anus — waste exit opening `[anus = ring (L)]`; villi / villus — finger-like gut projections `[villus = shaggy hair (L)]`; microvilli — tiny villi on cells `[micro- = small (Gk)]`; saliva — enzyme-containing mouth fluid `[saliva = spittle (L)]`; bile — liver fluid aiding digestion `[bilis (L)]`; faeces — solid body waste `[faex = dregs (L)]`; gland — organ that releases substances `[glans = acorn (L)]`; hydrochloric acid — stomach acid killing bacteria `[hydro- = water + chloros = green (Gk)]`.

### B3.3 Gut bacteria
**Tier 2:** function / functioning — the job something does `[fungi = perform (L)]`; process — a series of steps `[pro-cedere = go forward (L)]`; compete — to contend for resources `[com-petere = seek together (L)]`; produce — to make `[pro-ducere = lead forth (L)]`; harmful / helpful — causing harm / giving help `[—]`; balanced diet — well-proportioned nutrition `[—]`.
**Tier 3:** bacteria — single-celled microorganisms `[bakterion = little rod (Gk)]`; unicellular — made of one cell `[uni- = one + cella = chamber (L)]`; toxin — poison released by cells `[toxikon = poison (Gk)]`; flagellum — whip-like moving structure `[flagellum = whip (L)]`; capsule — protective bacterial coat `[capsa = box (L)]`; cytoplasm — jelly filling a cell `[kytos = cell + plasma = form (Gk)]`; nucleus — cell control centre `[nucleus = kernel (L)]`; fibre — indigestible gut-moving bulk `[fibra = thread (L)]`.

## B4 Gas exchange systems

### B4.1 Ventilation
**Tier 2:** ventilation / ventilate — moving air in and out `[ventus = wind (L)]`; system — parts working together `[syn-histanai = set up together (Gk)]`; structure — arrangement of parts `[struere = build (L)]`; unconscious(ly) — without awareness `[un- + conscius = aware (L)]`; contract — to shorten / tighten `[con-trahere = draw together (L)]`; expand / expanding — to grow larger `[ex-pandere = spread out (L)]`; volume — amount of space `[volumen = roll (L)]`; pressure — force over an area `[pressare = press (L)]`; inflate / deflate — fill / empty with air `[in-/de- + flare = blow (L)]`; protective — giving protection `[pro-tegere = cover (L)]`; represent — to stand for in a model `[re-praesentare = show (L)]`.
**Tier 3:** respiratory / respiration — breathing / gas use in cells `[re- + spirare = breathe (L)]`; trachea — the windpipe `[tracheia = rough (Gk)]`; bronchus / bronchi — main lung airways `[bronchos = windpipe (Gk)]`; bronchiole — small lung airway `[-ole = little (L)]`; alveoli / alveolus — microscopic air sacs `[alveolus = little cavity (L)]`; diaphragm — breathing muscle sheet `[dia- = across + phragma = wall (Gk)]`; intercostal — between the ribs `[inter- = between + costa = rib (L)]`; thorax — the chest cavity `[thorax = breastplate (Gk)]`; cartilage — flexible supportive tissue `[cartilago (L)]`; cilia — hair-like sweeping structures `[cilium = eyelash (L)]`; epithelial — surface-lining cells `[epi- = upon + thele = nipple (Gk)]`; goblet cell — mucus-making cell `[—]`; mucus — sticky trapping fluid `[mucus = slime (L)]`.

### B4.2 Gas exchange
**Tier 2:** exchange — swapping one for another `[ex- = out + cambiare = barter (L)]`; composition — what something is made of `[com-ponere = put together (L)]`; concentration — amount in a space `[con- + centrum = centre (L)]`; abundant — present in large amounts `[abundare = overflow (L)]`; maintain — to keep the same `[manu-tenere = hold in hand (L)]`; efficient — working with little waste `[ef-ficere = accomplish (L)]`; adaptation — feature suited to a function `[ad-aptare = fit to (L)]`; percentage — amount per hundred `[per centum = by the hundred (L)]`; pathway — route taken `[—]`.
**Tier 3:** diffusion / diffuse — spread from high to low concentration `[dif-fundere = pour apart (L)]`; alveolus / alveoli — gas-exchange air sacs `[alveolus = little cavity (L)]`; capillary — tiny blood vessel `[capillus = hair (L)]`; oxygen — gas taken in for respiration `[oxys = sharp + genes = forming (Gk)]`; carbon dioxide — waste gas breathed out `[di- = two + oxide (Gk)]`; nitrogen — main unused air gas `[nitron = soda + genes = forming (Gk)]`; argon — inert air gas `[argos = inactive (Gk)]`; water vapour — gaseous water exhaled `[vapor = steam (L)]`; concentration gradient — a difference in concentration `[gradi = to step (L)]`.

### B4.3 Exercise, asthma and smoking
**Tier 2:** affect — to have an effect on `[af-ficere = act on (L)]`; accumulate — to build up `[ad- + cumulare = heap (L)]`; inflamed / inflammation — swollen and irritated `[in-flammare = set on fire (L)]`; irritated — made sore or reactive `[irritare = provoke (L)]`; trigger — a thing that sets off `[—]`; manage — to control a condition `[maneggiare = handle (It/L)]`; addictive — causing dependence `[ad-dicere = give over (L)]`; reduce — to make less `[re-ducere = bring back (L)]`; persistent — continuing on `[per-sistere = stand firm (L)]`.
**Tier 3:** asthma — airway-narrowing condition `[asthma = panting (Gk)]`; bronchitis — inflammation of bronchi `[-itis = inflammation (Gk)]`; alveoli — gas-exchange sacs damaged by smoking `[alveolus = little cavity (L)]`; carcinogen — cancer-causing chemical `[karkinos = crab/cancer + genes = forming (Gk)]`; nicotine — addictive tobacco drug `[—]`; carbon monoxide — oxygen-blocking cigarette gas `[mono- = one + oxide (Gk)]`; tar — sticky harmful smoke residue `[—]`; cilia — mucus-sweeping hairs damaged by smoke `[cilium = eyelash (L)]`; inhaler — asthma-treatment device `[in-halare = breathe in (L)]`; breathing rate — breaths per unit time `[—]`; mucus — sticky airway fluid `[mucus = slime (L)]`.

## B5 Reproduction

### B5.1 Sexual reproduction and the reproductive organs
**Tier 2:** reproduce / reproduction — making offspring `[re- = again + producere = bring forth (L)]`; organ — body part with a function `[organon = tool (Gk)]`; specialised — adapted to a specific role `[species = kind (L)]`; determine(d) — to fix or decide `[de-terminare = set limits (L)]`; deliver — to carry to a place `[de-liberare = set free (L)]`; sustain — to keep going `[sus-tenere = hold up (L)]`; equivalent — matching in role `[aequi- = equal + valere = be worth (L)]`; internal — inside the body `[internus = inward (L)]`.
**Tier 3:** gamete — a sex cell `[gamete = wife / gamein = marry (Gk)]`; sperm — male gamete `[sperma = seed (Gk)]`; ovum / ova — female gamete `[ovum = egg (L)]`; fertilisation — fusing of gametes `[fertilis = fruitful (L)]`; offspring — the young produced `[—]`; mammal — milk-feeding live-bearing animal `[mamma = breast (L)]`; testis / testes — sperm-making organ `[testis (L)]`; scrotum — sac holding testes `[scrotum = pouch (L)]`; sperm duct — tube carrying sperm `[ducere = to lead (L)]`; urethra — tube carrying sperm/urine `[ourein = urinate (Gk)]`; penis — sperm-delivering organ `[penis = tail (L)]`; ovary / ovaries — ovum-releasing organ `[ovum = egg (L)]`; oviduct — tube carrying the ovum `[ovi- = egg + ducere = lead (L)]`; uterus — where the baby develops `[uterus = womb (L)]`; cervix — protective uterus entrance `[cervix = neck (L)]`; vagina — canal accepting the penis `[vagina = sheath (L)]`; seminal fluid — sperm-nourishing fluid `[semen = seed (L)]`; puberty — onset of reproductive ability `[pubertas = adulthood (L)]`.

### B5.2 Fertilisation
**Tier 2:** adapted / adaptation — suited to a function `[ad-aptare = fit to (L)]`; function — the job a cell does `[fungi = perform (L)]`; specialised — adapted to a role `[species = kind (L)]`; streamlined — shaped to move easily `[—]`; fuse / fusion — to join into one `[fundere = to pour/melt (L)]`; membrane — thin cell boundary `[membrana = skin (L)]`; concentration — amount in a space `[con- + centrum = centre (L)]`; changeable — able to alter `[—]`.
**Tier 3:** gamete — sex cell `[gamein = marry (Gk)]`; sperm — male gamete `[sperma = seed (Gk)]`; ovum — female gamete `[ovum = egg (L)]`; nucleus — DNA-holding cell centre `[nucleus = kernel (L)]`; mitochondria — energy-releasing organelles `[mitos = thread + chondrion = grain (Gk)]`; respiration — energy release in cells `[re- + spirare = breathe (L)]`; enzyme — protein aiding ovum entry `[en- + zyme = leaven (Gk)]`; cytoplasm — jelly filling a cell `[kytos = cell + plasma = form (Gk)]`; jelly coat — protective ovum layer `[—]`; fertilisation — fusion of gamete nuclei `[fertilis = fruitful (L)]`; zygote — first cell of new offspring `[zygotos = joined (Gk)]`; ovulation — release of the ovum `[ovum = egg (L)]`; DNA — genetic material `[—]`; gene — unit of inheritance `[genos = birth (Gk)]`.

### B5.3 Fetal development
**Tier 2:** develop / development — to grow and form `[—]`; support — to help sustain `[sup-portare = carry (L)]`; transfer / transferred — to move across `[trans- = across + ferre = carry (L)]`; exchange — to swap substances `[ex- + cambiare = barter (L)]`; depend — to rely on `[de-pendere = hang from (L)]`; connect — to join together `[con-nectere = bind (L)]`; specialise — to become a specific type `[species = kind (L)]`; implant — to embed into tissue `[in- + plantare = to plant (L)]`; cushion — to protect from impact `[—]`; concentration — amount in a space `[con- + centrum = centre (L)]`.
**Tier 3:** fetus — unborn young after 8 weeks `[fetus = offspring (L)]`; embryo — early dividing ball of cells `[en- = in + bryein = swell (Gk)]`; zygote — fertilised single cell `[zygotos = joined (Gk)]`; placenta — organ exchanging substances `[placenta = flat cake (Gk/L)]`; umbilical cord — blood-vessel link to placenta `[umbilicus = navel (L)]`; amnion / amniotic fluid — protective fluid sac `[amnion = fetal membrane (Gk)]`; gestation — pregnancy growth period `[gestare = to carry (L)]`; diffusion — substance movement down a gradient `[dif-fundere = pour apart (L)]`; uterus — where the fetus grows `[uterus = womb (L)]`; nutrient — substance nourishing the fetus `[nutrire = nourish (L)]`; urea — nitrogenous waste `[ouron = urine (Gk)]`.

### B5.4 The menstrual cycle
**Tier 2:** cycle — a repeating sequence `[kyklos = circle (Gk)]`; prepare / preparatory — to make ready `[prae-parare = make ready beforehand (L)]`; maintain / maintained — to keep the same `[manu-tenere = hold in hand (L)]`; shed — to lose or cast off `[—]`; trigger / triggered — to set off a process `[—]`; outcome — a result `[—]`; signal — a message sent `[signum = sign (L)]`; average — a typical middle value `[—]`.
**Tier 3:** menstruation / menstrual — shedding of uterus lining `[menstruus = monthly / mensis = month (L)]`; ovulation — release of the ovum `[ovum = egg (L)]`; ovum — female gamete `[ovum = egg (L)]`; zygote — fertilised cell `[zygotos = joined (Gk)]`; embryo — implanting ball of cells `[en- + bryein = swell (Gk)]`; uterus / uterus lining — muscular reproductive organ `[uterus = womb (L)]`; oviduct — tube carrying the ovum `[ovi- + ducere = lead (L)]`; implant / implantation — embedding of the embryo `[in- + plantare = to plant (L)]`; placenta — fetus-supporting organ `[placenta = flat cake (Gk/L)]`; hormone — chemical messenger `[horman = to set in motion (Gk)]`.

### B5.5 Plant reproduction
**Tier 2:** reproduce / reproduction — making offspring `[re- + producere = bring forth (L)]`; attract — to draw towards `[at-trahere = draw to (L)]`; disperse / dispersal — to spread apart `[dis- = apart + spargere = scatter (L)]`; structure — arrangement of parts `[struere = build (L)]`; deliver — to carry to a target `[de-liberare = set free (L)]`; simultaneously — at the same time `[simul = together (L)]`; adapt / adapted — suited to a function `[ad-aptare = fit to (L)]`; convention — an agreed practice `[con-venire = come together (L)]`; germinate / germination — to sprout and grow `[germen = sprout (L)]`.
**Tier 3:** pollen — powder holding male gamete `[pollen = fine flour (L)]`; pollinator / pollination — animal/process transferring pollen `[pollen = fine flour (L)]`; nectar — sugary flower fluid `[nektar (Gk)]`; gamete — plant sex cell `[gamein = marry (Gk)]`; ovum — female plant gamete `[ovum = egg (L)]`; ovule — structure holding the ovum `[ovulum = little egg (L)]`; ovary — holds ovules, becomes fruit `[ovum = egg (L)]`; anther — pollen-making structure `[anthera = flowery (Gk)]`; stigma — sticky pollen-catching tip `[stigma = mark (Gk)]`; style — tube from stigma to ovary `[stylos = pillar (Gk)]`; petal — colourful pollinator-attracting part `[petalon = leaf (Gk)]`; pollen tube — grows to deliver male gamete `[—]`; pollen grain — single pollen particle `[—]`; fertilisation — fusion of plant gametes `[fertilis = fruitful (L)]`; embryo — young plant in the seed `[en- + bryein = swell (Gk)]`; seed — embryo-protecting structure `[—]`; fruit — ripened ovary spreading seeds `[fructus = produce (L)]`; self-fertilise / cross-fertilise — fertilise self / another plant `[—]`; kernel — the inner part of a nut `[—]`.

## Biology — Year 3

## B6 Photosynthesis

### B6.1 Photosynthesis
**Tier 2:** absorb — take in light or water `[ab- = away (L); sorbere = to suck in]`; transfer — move energy from place to place `[trans- = across (L); ferre = to carry]`; produce — make a substance in a reaction `[pro- = forth (L); ducere = to lead]`; reactant — substance that goes into a reaction `[re- = again (L); agere = to act]`; product — substance made by a reaction `[pro- = forth (L); ducere = to lead]`; convert — change glucose into starch `[con- = together (L); vertere = to turn]`; store — keep glucose for later use `[—]`; process — a series of ordered steps `[pro- = forward (L); cedere = to go]`; structure — the arrangement of parts `[struere = to build (L)]`; function — the job a part does `[fungi = to perform (L)]`; equation — balanced reactant→product statement `[aequare = to make equal (L)]`; model — a scientific representation of an idea `[modulus = small measure (L)]`; evidence — facts supporting a conclusion `[e- = out (L); videre = to see]`; endothermic — takes in energy `[endo- = within (Gk); therme = heat]`; exothermic — gives out energy `[exo- = outside (Gk); therme = heat]`

**Tier 3:** photosynthesis — plants making food using light `[photo- = light (Gk); -synthesis = putting together]`; chlorophyll — green pigment that absorbs light `[chloros = green (Gk); phyllon = leaf]`; chloroplast — cell part where photosynthesis happens `[chloros = green (Gk); plastos = formed]`; glucose — sugar made in photosynthesis `[gleukos = sweet (Gk)]`; carbon dioxide — gas reactant of photosynthesis `[di- = two (Gk); oxys = sharp/acid]`; oxygen — gas product of photosynthesis `[oxys = sharp (Gk); -gen = producing]`; palisade cell — tall leaf cell packed with chloroplasts `[palus = stake (L)]`; starch — storage carbohydrate in plants `[—]`; carbohydrate — sugar/starch energy molecule `[carbo = coal (L); hydor = water (Gk)]`; pigment — substance that gives colour `[pigmentum = paint (L)]`; iodine — reagent used to test for starch `[iodes = violet (Gk)]`; algae — simple water-living green organisms `[alga = seaweed (L)]`; unicellular — made of a single cell `[uni- = one (L); cellula = little room]`; respiration — releasing energy from glucose `[re- = again (L); spirare = to breathe]`; mitochondria — cell part where respiration occurs `[mitos = thread (Gk); chondrion = grain]`

### B6.2 Leaf structure
**Tier 2:** adapt — be suited to a function `[ad- = to (L); aptare = to fit]`; structure — internal arrangement of leaf layers `[struere = to build (L)]`; surface area — outer area exposed for absorption `[super- = above (L); facies = face]`; concentration — amount of substance per space `[con- = together (L); centrum = centre]`; transparent — lets light pass through `[trans- = through (L); parere = to appear]`; magnify — make appear larger `[magnus = great (L); facere = to make]`; layer — a sheet of tissue in the leaf `[—]`

**Tier 3:** diffusion — movement from high to low concentration `[dis- = apart (L); fundere = to pour]`; stomata — leaf pores for gas exchange `[stoma = mouth (Gk)]`; guard cell — cell that opens/closes a stoma `[—]`; epidermis — outer covering layer of the leaf `[epi- = upon (Gk); derma = skin]`; mesophyll — middle photosynthetic leaf tissue `[mesos = middle (Gk); phyllon = leaf]`; spongy mesophyll — airy leaf layer for gas diffusion `[mesos = middle (Gk); phyllon = leaf]`; palisade mesophyll — dense upper layer for photosynthesis `[palus = stake (L)]`; cuticle — waxy waterproof leaf covering `[cutis = skin (L)]`; chloroplast — organelle containing chlorophyll `[chloros = green (Gk); plastos = formed]`; microscope — instrument for viewing tiny objects `[mikros = small (Gk); skopein = to look]`; magnification — factor by which an image is enlarged `[magnus = great (L); facere = to make]`; variegated — leaf with green and non-green parts `[varius = varied (L)]`

## B7 Respiration

### B7.1 Aerobic respiration
**Tier 2:** release — give out energy `[re- = back (L); laxare = to loosen]`; transfer — move energy to another store `[trans- = across (L); ferre = to carry]`; reactant — substance entering the reaction `[re- = again (L); agere = to act]`; product — substance made in the reaction `[pro- = forth (L); ducere = to lead]`; function — main job of an organ `[fungi = to perform (L)]`; process — one of the life processes `[pro- = forward (L); cedere = to go]`; absorb — take glucose into the body `[ab- = away (L); sorbere = to suck in]`; equation — reactant→product statement `[aequare = to make equal (L)]`; availability — how much of a substance is present `[ad- = to (L); valere = to be worth]`; investigate — carry out an inquiry `[in- = into (L); vestigare = to track]`

**Tier 3:** aerobic — respiration that requires oxygen `[aero- = air (Gk); bios = life]`; respiration — releasing energy from glucose `[re- = again (L); spirare = to breathe]`; glucose — sugar reactant of respiration `[gleukos = sweet (Gk)]`; oxygen — gas reactant of aerobic respiration `[oxys = sharp (Gk); -gen = producing]`; carbon dioxide — waste gas product of respiration `[di- = two (Gk); oxys = acid]`; mitochondria — site of respiration in cells `[mitos = thread (Gk); chondrion = grain]`; cytoplasm — jelly filling the cell `[kytos = vessel (Gk); plasma = formed]`; enzyme — protein that breaks down molecules `[en- = in (Gk); zyme = leaven]`; membrane — thin cell boundary layer `[membrana = skin (L)]`; diffusion — movement down a concentration gradient `[dis- = apart (L); fundere = to pour]`; organism — a living thing `[organon = tool/organ (Gk)]`; yeast — a unicellular fungus `[—]`; fungus — non-plant, non-animal organism `[fungus = mushroom (L)]`; unicellular — made of one cell `[uni- = one (L); cellula = little room]`

### B7.2 Anaerobic respiration
**Tier 2:** compare — note similarities and differences `[com- = with (L); par = equal]`; contrast — show the differences between `[contra- = against (L); stare = to stand]`; convert — change glucose into other products `[con- = together (L); vertere = to turn]`; evolve — develop different versions over time `[e- = out (L); volvere = to roll]`; version — a differing form of a process `[vertere = to turn (L)]`; product — substance made in the reaction `[pro- = forth (L); ducere = to lead]`; reactant — substance entering the reaction `[re- = again (L); agere = to act]`; advantage — benefit of one process over another `[ab-ante = in front (L)]`

**Tier 3:** anaerobic — respiration without oxygen `[an- = without (Gk); aero- = air; bios = life]`; fermentation — anaerobic respiration in yeast `[fermentum = leaven (L)]`; lactic acid — product of anaerobic respiration in animals `[lac = milk (L)]`; ethanol — alcohol produced by fermentation `[ether + -ol = alcohol suffix]`; glucose — reactant of anaerobic respiration `[gleukos = sweet (Gk)]`; carbon dioxide — gas produced in fermentation `[di- = two (Gk); oxys = acid]`; yeast — fungus that ferments `[—]`; cytoplasm — where anaerobic respiration occurs `[kytos = vessel (Gk); plasma = formed]`; mitochondrion — organelle of aerobic respiration `[mitos = thread (Gk); chondrion = grain]`; raising agent — substance making dough rise `[—]`

## B8 Ecosystems

### B8.1 Food chains
**Tier 2:** transfer — flow of energy along a chain `[trans- = across (L); ferre = to carry]`; relationship — link between feeding organisms `[re- = back (L); ferre = to carry]`; direction — the way arrows point `[dirigere = to guide (L)]`; consume — eat another organism `[con- = wholly (L); sumere = to take]`; nutrient — substance an organism needs `[nutrire = to nourish (L)]`; concentration — amount of chemical present `[con- = together (L); centrum = centre]`; accumulate — build up over time `[ad- = to (L); cumulus = heap]`; source — where energy comes from `[surgere = to rise (L)]`

**Tier 3:** ecosystem — community of organisms and environment `[oikos = house (Gk); systema = organised whole]`; food chain — sequence of feeding links `[—]`; producer — organism that makes its own food `[pro- = forth (L); ducere = to lead]`; consumer — organism that eats others `[con- = wholly (L); sumere = to take]`; organism — a living thing `[organon = tool/organ (Gk)]`; photosynthesis — process producers use to make food `[photo- = light (Gk); synthesis = putting together]`; bioaccumulation — build-up of chemicals up a chain `[bios = life (Gk); ad-cumulus = heaped]`; pesticide — chemical that kills pests `[pestis = plague (L); -cide = to kill]`; insecticide — chemical that kills insects `[insectum = cut into (L); -cide = to kill]`; phytoplankton — tiny water-living green organisms `[phyton = plant (Gk); planktos = drifting]`; predator — animal that hunts prey `[praeda = prey (L)]`

### B8.2 Food webs and interdependence
**Tier 2:** interdependence — mutual reliance between organisms `[inter- = between (L); de-pendere = to hang from]`; depend — rely on another organism `[de- = down (L); pendere = to hang]`; impact — effect of a change `[im- = into (L); pangere = to fix]`; population — number of a species in an area `[populus = people (L)]`; estimate — work out an approximate number `[aestimare = to value (L)]`; sample — take a small measured portion `[exemplum = example (L)]`; approximation — a close-enough figure `[ad- = to (L); proximus = nearest]`; category — a group things are sorted into `[kategoria = statement (Gk)]`; adapt — be suited to an environment `[ad- = to (L); aptare = to fit]`; cycle — a repeating pattern over time `[kyklos = circle (Gk)]`; balance — an overall steady state `[bi- = two (L); lanx = scale-pan]`; maintain — keep a state steady `[manu = by hand (L); tenere = to hold]`; environment — the surroundings of an organism `[en- = in (Fr); viron = circuit]`

**Tier 3:** food web — interconnected food chains `[—]`; herbivore — animal that eats only plants `[herba = plant (L); vorare = to devour]`; carnivore — animal that eats only meat `[carn- = flesh (L); vorare = to devour]`; omnivore — animal that eats plants and meat `[omni- = all (L); vorare = to devour]`; predator — animal that hunts others `[praeda = prey (L)]`; prey — animal hunted for food `[prehendere = to seize (L)]`; producer — organism making its own food `[pro- = forth (L); ducere = to lead]`; consumer — organism that eats others `[con- = wholly (L); sumere = to take]`; species — a group that can interbreed `[species = kind (L)]`; quadrat — square frame for sampling `[quadratus = squared (L)]`; camouflage — colouring that hides an organism `[camoufler = to disguise (Fr)]`; insulation — layer reducing heat loss `[insula = island (L)]`

### B8.3 Insect pollination and food security
**Tier 2:** security — reliable supply of food `[se- = free from (L); cura = care]`; impact — effect on food supply `[im- = into (L); pangere = to fix]`; population — number of bees in an area `[populus = people (L)]`; destruction — the destroying of habitats `[de- = down (L); struere = to build]`; produce — make fruits and seeds `[pro- = forth (L); ducere = to lead]`; increase — grow larger in number `[in- = in (L); crescere = to grow]`; decline — fall in number over time `[de- = down (L); clinare = to lean]`; fluctuate — vary up and down `[fluctus = wave (L)]`; toxic — poisonous to bees `[toxikon = poison (Gk)]`; sustain — support life or numbers `[sub- = under (L); tenere = to hold]`

**Tier 3:** pollination — moving pollen between flowers `[pollen = fine dust (L); -ation = process]`; pollen — plant male gamete cells `[pollen = fine dust (L)]`; gamete — a sex cell `[gamete = wife (Gk)]`; fertilisation — fusion of gametes `[fertilis = fruitful (L)]`; ovum — female sex cell of a plant/animal `[ovum = egg (L)]`; ovary — flower part that grows into fruit `[ovum = egg (L)]`; stamen — pollen-bearing flower part `[stamen = thread (L)]`; nectar — sweet liquid that attracts bees `[nektar = drink of the gods (Gk)]`; germinate — begin to grow from a seed `[germen = sprout (L)]`; habitat — natural home of an organism `[habitare = to dwell (L)]`; pesticide — chemical that kills pests `[pestis = plague (L); -cide = to kill]`; neonicotinoid — pesticide toxic to bees `[neo- = new (Gk); nicotine + -oid = like]`; colony — a group of bees living together `[colonia = settlement (L)]`

## B9 Inheritance

### B9.1 DNA and chromosomes
**Tier 2:** inherit — receive characteristics from parents `[in- = in (L); heres = heir]`; transmit — pass information to the next generation `[trans- = across (L); mittere = to send]`; generation — a step in a family line `[generare = to beget (L)]`; characteristic — an inherited feature `[charakter = stamp (Gk)]`; structure — the shape of DNA `[struere = to build (L)]`; distinctive — clearly recognisable `[distinguere = to separate (L)]`; code — instructions carried by a gene `[codex = book (L)]`; combination — a particular mix of features `[com- = together (L); bini = two by two]`; source — where information comes from `[surgere = to rise (L)]`; process — steps of heredity `[pro- = forward (L); cedere = to go]`; publish — make findings public `[publicare = to make public (L)]`; review — check another's work `[re- = again (L); videre = to see]`

**Tier 3:** DNA — molecule carrying genetic code `[deoxyribonucleic acid]`; chromosome — coiled thread of DNA `[chroma = colour (Gk); soma = body]`; gene — DNA section coding for a protein `[genos = birth (Gk)]`; nucleus — cell part holding genetic material `[nucleus = kernel (L)]`; genetic — relating to genes `[genos = birth (Gk)]`; heredity — passing of genes across generations `[heres = heir (L)]`; double helix — twisted-ladder shape of DNA `[helix = spiral (Gk)]`; protein — molecule coded for by a gene `[proteios = primary (Gk)]`; gamete — sex cell with half the DNA `[gamete = wife (Gk)]`; sperm — male sex cell `[sperma = seed (Gk)]`; ovum — female sex cell `[ovum = egg (L)]`; zygote — cell formed at fertilisation `[zygotos = joined (Gk)]`; fertilisation — fusion of sperm and ovum `[fertilis = fruitful (L)]`; offspring — product of reproduction `[—]`; fetus — developing unborn organism `[fetus = offspring (L)]`; peer review — checking of findings by other scientists `[par = equal (L)]`

### B9.2 Variation
**Tier 2:** variation — differences within a population `[variare = to change (L)]`; vary — to differ `[varius = varied (L)]`; continuous — falling anywhere in a range `[con- = together (L); tenere = to hold]`; discontinuous — falling into separate groups `[dis- = apart (L); continuous]`; category — a separate group `[kategoria = statement (Gk)]`; range — spread between highest and lowest `[rang = row (Fr)]`; trend — a pattern in data `[trendan = to turn (OE)]`; environment — surroundings affecting an organism `[en- = in (Fr); viron = circuit]`; population — the group being studied `[populus = people (L)]`; combination — mix of genes and environment `[com- = together (L); bini = two by two]`; characteristic — a measurable feature `[charakter = stamp (Gk)]`; distribution — how values are spread `[dis- = apart (L); tribuere = to allot]`

**Tier 3:** continuous variation — variation over a smooth range `[con- = together (L); tenere = to hold]`; discontinuous variation — variation in distinct categories `[dis- = apart (L)]`; gene — unit of inherited information `[genos = birth (Gk)]`; DNA — the genetic material `[deoxyribonucleic acid]`; inherited characteristic — feature passed from parents `[heres = heir (L)]`; blood group — inherited blood category `[—]`; heredity — inheritance of genes `[heres = heir (L)]`; bar chart — graph for discontinuous data `[—]`

### B9.3 Competition and natural selection
**Tier 2:** compete — struggle for limited resources `[com- = together (L); petere = to seek]`; competition — the struggle for resources `[com- = together (L); petere = to seek]`; select — favour some organisms over others `[se- = apart (L); legere = to choose]`; survive — stay alive `[super- = beyond (L); vivere = to live]`; reproduce — produce offspring `[re- = again (L); pro-ducere = to lead forth]`; resource — something needed to survive `[re- = again (L); surgere = to rise]`; adapt — be suited to an environment `[ad- = to (L); aptare = to fit]`; adaptation — a feature aiding survival `[ad- = to (L); aptare = to fit]`; environment — an organism's surroundings `[en- = in (Fr); viron = circuit]`; population — group of one species `[populus = people (L)]`; generation — a step in the line of descent `[generare = to beget (L)]`; successful — able to compete well `[succedere = to come after (L)]`; variation — differences within a population `[variare = to change (L)]`; misconception — a mistaken belief `[mis- = wrong; con-capere = to take in]`

**Tier 3:** natural selection — survival of the best-adapted `[selectio = a choosing (L)]`; evolution — change in a population over time `[e- = out (L); volvere = to roll]`; adaptation — inherited survival feature `[ad- = to (L); aptare = to fit]`; camouflage — colouring that hides an organism `[camoufler = to disguise (Fr)]`; species — group that can interbreed `[species = kind (L)]`; naturalist — scientist who studies nature `[natura = nature (L)]`; genes — inherited units passed to offspring `[genos = birth (Gk)]`; offspring — the young produced `[—]`; photosynthesise — make food using light `[photo- = light (Gk); synthesis = putting together]`; territory — area an animal defends `[terra = land (L)]`; predator — animal that hunts prey `[praeda = prey (L)]`

### B9.4 Biodiversity
**Tier 2:** classify — sort into groups `[classis = class (L); facere = to make]`; classification — the system of grouping `[classis = class (L)]`; category — a group of organisms `[kategoria = statement (Gk)]`; vulnerability — how easily harmed `[vulnerare = to wound (L)]`; sustain — support life or numbers `[sub- = under (L); tenere = to hold]`; conserve — protect from loss `[con- = together (L); servare = to keep]`; conservation — protection of habitats `[con- = together (L); servare = to keep]`; introduce — bring a new species into an area `[intro- = inwards (L); ducere = to lead]`; reduce — fall in number `[re- = back (L); ducere = to lead]`; maintain — keep biodiversity steady `[manu = by hand (L); tenere = to hold]`; uniform — all of one kind `[uni- = one (L); forma = form]`; range — the variety present `[rang = row (Fr)]`; population — number of a species `[populus = people (L)]`; pollution — contamination of the environment `[polluere = to defile (L)]`

**Tier 3:** biodiversity — range of species in an area `[bios = life (Gk); di-vertere = to turn apart]`; species — group that produces fertile offspring `[species = kind (L)]`; kingdom — top level of classification `[—]`; vertebrate — animal with a backbone `[vertebra = joint (L)]`; invertebrate — animal without a backbone `[in- = not (L); vertebra = joint]`; mammal — vertebrate group including humans `[mamma = breast (L)]`; reptile — vertebrate group like snakes `[reptilis = creeping (L)]`; amphibian — vertebrate living in water and land `[amphi- = both (Gk); bios = life]`; prokaryote — kingdom of bacteria `[pro- = before (Gk); karyon = kernel/nucleus]`; protoctist — kingdom of amoeba-like organisms `[protos = first (Gk); ktistos = created]`; fungi — kingdom including yeast `[fungus = mushroom (L)]`; bacteria — single-celled prokaryotes `[bakterion = little rod (Gk)]`; unicellular — made of one cell `[uni- = one (L); cellula = little room]`; monoculture — growing a single crop `[mono- = one (Gk); cultura = cultivation]`; deforestation — cutting down of forests `[de- = down (L); forest]`; afforestation — establishing new forest `[ad- = to (L); forest]`; habitat — natural home of an organism `[habitare = to dwell (L)]`; extinct — no members of a species remain `[ex- = out (L); stinguere = to quench]`; endangered — at serious risk of extinction `[en- = in (Fr); danger]`; infertile — unable to produce offspring `[in- = not (L); fertilis = fruitful]`; hybrid — offspring of two different species `[hybrida = mongrel (L)]`; gene bank — store of organisms' DNA `[genos = birth (Gk)]`; breeding programme — controlled breeding to raise numbers `[—]`; classification — grouping of organisms `[classis = class (L)]`


---

# CHEMISTRY

## Chemistry — Year 1

## C1 The particle model

### C1.1 Simple particle model
**Tier 2:** matter — anything made of particles `[—]`; property — a feature/behaviour of a substance `[propri- = one's own (L)]`; state — a form of matter (solid/liquid/gas) `[stat- = standing/condition (L)]`; particle — tiny unit that makes up matter `[part- = a portion (L); -icle = little]`; model — simplified representation of an idea `[modul- = measure/pattern (L)]`; motion — movement `[mot- = to move (L)]`; perpetual — never-ending, continuous `[per- = through (L); -petu- = to seek/rush]`; arrange — to set in an order/pattern `[a- = to (L); range = row/rank (Fr)]`; arrangement — the way particles are ordered `[—]`; regular — evenly ordered, following a pattern `[reg- = to rule/straighten (L)]`; vibrate — to move quickly to and fro `[vibr- = to shake (L)]`; force — a push or pull `[fort- = strong (L)]`; attraction — a pulling-together force `[trac- = to pull/drag (L); ad- = towards]`; represent — to stand for/show `[re- = again; praesent- = to present (L)]`.

**Tier 3:** solid — state with fixed shape and volume `[solid- = firm/dense (L)]`; liquid — state that flows and takes container shape `[liqu- = to be fluid (L)]`; gas — state that fills its container `[from Gk khaos = void]`.

### C1.2 Properties of different states of matter
**Tier 2:** property — feature/behaviour of a substance `[propri- = one's own (L)]`; behaviour — how something acts `[—]`; structure — how parts are arranged `[stru- = to build (L)]`; bonding — the joining/holding of particles `[—]`; conduct — to carry (e.g. electricity) `[con- = with; duc- = to lead (L)]`; random — with no fixed order `[—]`; abstract — not concrete, hard to picture `[abs- = away; trac- = to draw (L)]`; compress — to squeeze into less space `[com- = together; press- = to press (L)]`; compression — the act of squeezing smaller `[com- = together; press- = to press (L)]`; convert — to change from one form to another `[con- = together; vert- = to turn (L)]`; consequence — a result/effect `[con- = with; sequ- = to follow (L)]`; energy — capacity to do work/cause change `[en- = in; erg- = work (Gk)]`.

**Tier 3:** fluid — a substance (liquid or gas) that can flow `[flu- = to flow (L)]`; solid — state with no gaps, keeps shape `[solid- = firm (L)]`; liquid — flowing state `[liqu- = fluid (L)]`.

### C1.3 Changes of state
**Tier 2:** transition — a change from one state to another `[trans- = across; it- = to go (L)]`; process — a series of steps/changes `[pro- = forward; cess- = to go (L)]`; heat — thermal energy transferred `[—]`; temperature — how hot or cold something is `[temper- = to mix/moderate (L)]`; energy — capacity to cause change `[en- = in; erg- = work (Gk)]`; internal — inside `[intern- = inward (L)]`; surface — the outer layer/top `[sur- = above; face = form (L)]`; specific — for a particular case `[speci- = kind/appearance (L)]`.

**Tier 3:** melting — solid changing to liquid `[—]`; freezing — liquid changing to solid `[—]`; boiling — liquid to gas throughout, at boiling point `[—]`; condensing — gas changing to liquid `[con- = together; dens- = to thicken (L)]`; condensation — the change from gas to liquid `[con- = together; dens- = to thicken (L)]`; evaporation — liquid to gas at the surface, below boiling point `[e- = out; vapor = steam (L)]`; evaporate — to turn to vapour from the surface `[e- = out; vapor = steam (L)]`; melting point — temperature at which a substance melts `[—]`; boiling point — temperature at which a substance boils `[—]`.

### C1.4 Gas pressure
**Tier 2:** pressure — force pushing on a surface `[press- = to press (L)]`; collide — to hit/bump into `[col- = together; laed- = to strike (L)]`; collision — an impact between particles `[col- = together; laed- = to strike (L)]`; volume — the amount of space taken up `[volumen = a roll/bulk (L)]`; increase — to make larger `[in- = into; cresc- = to grow (L)]`; decrease — to make smaller `[de- = down; cresc- = to grow (L)]`; predict — to say what will happen `[prae- = before; dic- = to say (L)]`; condition — a factor/circumstance affecting something `[con- = with; dic- = to say (L)]`; container — a vessel that holds something `[con- = together; ten- = to hold (L)]`; frequently — happening often `[frequent- = crowded/repeated (L)]`.

**Tier 3:** gas pressure — force of gas particles hitting a container `[—]`.

## C2 Atoms, elements and compounds

### C2.1 The atomic model
**Tier 2:** model — simplified representation `[modul- = measure/pattern (L)]`; structure — how parts are arranged `[stru- = to build (L)]`; theory — a well-supported explanation `[theor- = to look at/contemplate (Gk)]`; layout — how things are set out `[—]`; arrange — to order/set out `[a- = to; range = row (Fr)]`; column — a vertical line/list `[column- = pillar (L)]`; row — a horizontal line `[—]`; indivisible — cannot be split `[in- = not; di- = apart; vid- = to divide (L)]`; calculate — to work out by maths `[calcul- = pebble used in counting (L)]`; fundamental — basic, forming the foundation `[fund- = bottom/base (L)]`; unit — a single whole thing `[unit- = oneness (L)]`; naturally — occurring in nature `[nat- = to be born (L)]`.

**Tier 3:** atom — smallest unit of matter that exists on its own `[a- = not; tom- = to cut (Gk) → uncuttable]`; element — substance made of only one type of atom `[element- = first principle (L)]`; compound — substance of two+ atom types bonded `[com- = together; pon- = to put (L)]`; proton — positive subatomic particle in the nucleus `[prot- = first (Gk)]`; neutron — neutral subatomic particle in the nucleus `[neutr- = neither (L)]`; electron — particle that orbits the nucleus `[elect- = amber/electricity (Gk)]`; subatomic — smaller than an atom `[sub- = under/below (L)]`; nucleus — the centre of an atom `[nucle- = kernel/little nut (L)]`; Periodic Table — chart listing all the elements `[peri- = around; hodos = way (Gk) → recurring]`; group — a vertical column of the Periodic Table `[—]`; period — a horizontal row of the Periodic Table `[peri- = around; hodos = way (Gk)]`; atomic number — number of protons in an atom `[—]`; atomic mass — protons plus neutrons in an atom `[—]`; electron shell — a region where electrons orbit `[—]`; electron configuration — how electrons are arranged in shells `[con- = together; figur- = to shape (L)]`; nuclide notation — way of writing an atom's numbers `[nucle- = kernel (L)]`.

### C2.2 Symbols and formulae
**Tier 2:** symbol — a letter/sign standing for something `[sym- = together; bol- = to throw (Gk)]`; formula — a set way of representing something `[form- = shape (L); -ula = little]`; represent — to stand for/show `[re- = again; praesent- (L)]`; ratio — the relative amounts of things `[rat- = to reckon/count (L)]`; combination — a joining together `[com- = with; bin- = two (L)]`; universal — used everywhere by everyone `[uni- = one; vers- = to turn (L)]`; subscript — written small and below the line `[sub- = under; scrib- = to write (L)]`; system — an organised set of rules `[syst- = to set together (Gk)]`; total — the whole amount `[tot- = all (L)]`.

**Tier 3:** chemical symbol — one/two letters for a type of atom `[chem- = alchemy (Gk/Ar)]`; chemical formula — symbols and numbers showing a substance `[—]`; molecule — a few atoms chemically joined `[mol- = mass (L); -cule = little]`; bubble diagram — drawing of atoms as circles `[dia- = through; gram- = to write (Gk)]`; atom — smallest unit of matter `[a- = not; tom- = to cut (Gk)]`; element — one-atom-type substance `[element- = first principle (L)]`; compound — bonded multi-atom substance `[com- = together; pon- = to put (L)]`.

### C2.3 Elements and compounds
**Tier 2:** property — feature/behaviour of a substance `[propri- = one's own (L)]`; physical — to do with appearance/state, not reactions `[phys- = nature (Gk)]`; chemical — to do with how substances react `[chem- (Gk/Ar)]`; classify — to sort into groups `[class- = group; -fy = to make (L)]`; combine — to join together `[com- = with; bin- = two (L)]`; react — to interact and change chemically `[re- = back; ag- = to act (L)]`; reaction — a chemical change `[re- = back; ag- = to act (L)]`; characteristic — a distinguishing feature `[charact- = a mark (Gk)]`; hazard — something that could cause harm `[from Ar az-zahr = the dice → chance/risk]`; risk — the chance of harm happening `[—]`; precaution — action taken to stay safe `[prae- = before; caut- = to be wary (L)]`; systematically — done in an ordered way `[syst- = to set together (Gk)]`; appearance — how something looks `[ap- = to; par- = to show (L)]`; dissolve — to mix a solid into a liquid `[dis- = apart; solv- = to loosen (L)]`; brittle — hard but easily broken `[—]`; corrosive — able to wear away/damage `[cor- = thoroughly; rod- = to gnaw (L)]`; irritant — something causing mild damage/irritation `[irrit- = to provoke (L)]`; dilute — weakened by adding water `[di- = apart; lu- = to wash (L)]`; concentrated/strong — high amount of substance present `[con- = together; centr- = centre (L)]`.

**Tier 3:** element — one-atom-type substance `[element- = first principle (L)]`; compound — bonded multi-atom substance `[com- = together; pon- = to put (L)]`; molecule — few atoms joined `[mol- = mass (L); -cule = little]`; oxide — compound of an element with oxygen `[-ide = simple compound]`; monoxide — compound with one oxygen atom `[mono- = one (Gk)]`; dioxide — compound with two oxygen atoms `[di- = two (Gk)]`; carbonate — salt containing carbon and oxygen `[-ate = oxygen-containing salt]`; nitrate — salt containing nitrogen and oxygen `[-ate = oxygen-containing salt]`; hydroxide — compound containing OH `[hydro- = water (Gk); -ide]`; paramagnetic — weakly attracted by a magnet `[para- = beside (Gk); magnet-]`; magnetic — attracted by a magnet `[from Magnesia, a place (Gk)]`; sulfide — compound of an element with sulfur `[-ide = simple compound]`.

## C3 Pure and impure substances

### C3.1 Diffusion
**Tier 2:** concentration — how crowded particles are in a space `[con- = together; centr- = centre (L)]`; random — with no fixed order/direction `[—]`; spread — to move out and cover space `[—]`; movement — motion of particles `[mov- = to move (L)]`; area — a region of space `[area = open space (L)]`; system — a defined set-up being studied `[syst- = to set together (Gk)]`; mechanism — a process/means by which something works `[mechan- = machine/device (Gk)]`; propel — to push/drive forward `[pro- = forward; pel- = to drive (L)]`.

**Tier 3:** diffusion — spread of particles from high to low concentration `[dis- = apart; fus- = to pour (L)]`; kinetic theory — idea that particles are always moving `[kine- = to move (Gk)]`.

### C3.2 Pure and impure
**Tier 2:** pure — made of only one type of particle `[pur- = clean (L)]`; impure — containing more than one type of particle `[im- = not; pur- = clean (L)]`; dissolve — to mix a solid into a liquid `[dis- = apart; solv- = to loosen (L)]`; mixture — substances together but not bonded `[mixt- = to mix (L)]`; separate — to divide/take apart `[se- = apart; par- = to make ready (L)]`; magnify — to make appear larger `[magn- = great (L); -fy = to make]`; volume — amount of space/liquid `[volumen = a roll/bulk (L)]`; identify — to work out what something is `[ident- = same (L); -fy = to make]`; definition — an exact statement of meaning `[de- = completely; fin- = to bound (L)]`; substance — a type of matter/material `[sub- = under; stant- = to stand (L)]`.

**Tier 3:** solution — a substance dissolved in another `[solv- = to loosen (L)]`; solute — the substance that dissolves `[solut- = loosened (L)]`; solvent — the substance that does the dissolving `[solv- = to loosen (L)]`; soluble — able to dissolve in a solvent `[solv- = to loosen (L); -ble = able]`; insoluble — unable to dissolve `[in- = not; solv- = to loosen (L)]`; solubility — how much/easily a substance dissolves `[solv- = to loosen (L); -ility = quality of]`; molecule — few atoms joined `[mol- = mass (L); -cule = little]`; compound — bonded multi-atom substance `[com- = together; pon- = to put (L)]`.

### C3.3 Separation
**Tier 2:** separate — to divide/take apart `[se- = apart; par- = to make ready (L)]`; separation — the act of dividing a mixture `[se- = apart; par- (L)]`; technique — a method of doing something `[techn- = art/skill (Gk)]`; apparatus — the equipment for an experiment `[ad- = to; par- = to prepare (L)]`; terminology — the correct technical words `[termin- = boundary (L); -logy = study of]`; method — an ordered way of doing something `[meta- = after; hodos = way (Gk)]`; function — the job/role of something `[funct- = to perform (L)]`; identify — to work out what something is `[ident- = same (L); -fy = to make]`; process — a series of steps `[pro- = forward; cess- = to go (L)]`; collect — to gather together `[col- = together; lect- = to gather (L)]`; boiling point — temperature at which a liquid boils `[—]`.

**Tier 3:** filtration — separating an insoluble solid from a liquid `[filtr- = felt/strainer (L)]`; filter — material with holes that traps large particles `[filtr- = felt/strainer (L)]`; filtrate — the liquid that passes through the filter `[filtr- + -ate]`; residue — the solid left in the filter paper `[resid- = to remain/sit back (L)]`; evaporation — turning a liquid to gas below boiling point `[e- = out; vapor = steam (L)]`; crystallisation — forming crystals as a solvent evaporates `[crystall- = ice/clear (Gk)]`; evaporating basin — dish used to evaporate a solvent `[e- = out; vapor = steam (L)]`; distillation — separating liquids by different boiling points `[de- = down; still- = to drip (L)]`; condenser — apparatus that cools gas back to liquid `[con- = together; dens- = to thicken (L)]`; condensation — gas turning back to liquid `[con- = together; dens- = to thicken (L)]`; chromatography — separating a mixture by different solubilities `[chroma- = colour; graph- = to write (Gk)]`; chromatogram — the finished paper showing separated colours `[chroma- = colour; gram- = to write (Gk)]`; solvent front — the line the solvent reaches on the paper `[solv- = to loosen (L)]`; soluble — able to dissolve `[solv- = to loosen (L); -ble = able]`; insoluble — unable to dissolve `[in- = not; solv- (L)]`; solution — dissolved-substance mixture `[solv- = to loosen (L)]`; solute — the dissolving substance `[solut- = loosened (L)]`.

## Chemistry — Year 2

## C4 Chemical reactions

### C4.1 Reactions, conventions and signs a reaction has occurred
**Tier 2:** react — to interact and change chemically `[re- = again/back (L)]`; form — to make/create a substance `[—]`; represent — to stand for/describe `[re- = again; present = show (L)]`; convention — an agreed way of writing `[con- = together; ven = come (L)]`; substitute — put one chemical in place of another `[sub- = under; stitu = set (L)]`; property — a characteristic of a substance `[—]`; separate — split a mixture apart `[se- = apart; par = prepare (L)]`; produce — to make/generate `[pro- = forth; duc = lead (L)]`; convert — change one thing into another `[con- = together; vert = turn (L)]`; reverse — change back to the original `[re- = back; vers = turn (L)]`; indicate — to signal/show `[indic = point out (L)]`

**Tier 3:** reactant — starting chemical on the left of arrow `[re- = again; -ant = agent (L)]`; product — chemical formed on the right `[pro- = forth; duc = lead (L)]`; compound — substance of atoms chemically joined `[com- = together; pon = put (L)]`; element — substance of one type of atom `[—]`; molecule — group of bonded atoms `[-cule = small (L)]`; word equation — reaction written in names `[aequa = equal (L)]`; symbol equation — reaction written in formulae `[symbol = token (Gk)]`; chemical formula — symbols showing atom types/numbers `[form = shape (L)]`; catalyst — speeds a reaction without being used up `[cata- = down (Gk); lysis = loosening]`; enzyme — biological catalyst `[en- = in (Gk); zyme = leaven]`; state symbol — letter showing solid/liquid/gas/aqueous `[—]`; aqueous — dissolved in water `[aqua = water (L)]`; solution — solute dissolved in solvent `[solu = loosen (L)]`; solute — substance that dissolves `[solu = loosen (L)]`; solvent — liquid that dissolves the solute `[solv = loosen (L)]`; mixture — substances not chemically joined `[mix = mingle (L)]`; effervescence — fizzing as gas is produced `[ef- = out; fervesc = boil (L)]`; precipitation — solid forming in a solution `[prae- = before; cipit = headlong (L)]`; precipitate — the solid that forms `[prae- = before; cipit = headlong (L)]`; universal indicator — dye showing pH by colour `[—]`; melting point — temperature solid becomes liquid `[—]`

### C4.2 Combustion, thermal decomposition, oxidation and displacement
**Tier 2:** react — to combine chemically `[re- = again (L)]`; represent — describe with an equation `[re- = again; present = show (L)]`; substitute — swap a specific chemical into a general equation `[sub- = under; stitu = set (L)]`; general — applying to a whole type `[gener = kind (L)]`; specific — a particular named example `[speci = kind (L)]`; produce — to form as a product `[pro- = forth; duc = lead (L)]`; convert — change into another substance `[con- = together; vert = turn (L)]`; source — where something comes from `[surg = rise (L)]`; component — a part of a whole `[com- = together; pon = put (L)]`; require — to need `[re- = again; quir = seek (L)]`; extract — remove/pull out `[ex- = out; tract = pull (L)]`; extrapolate — extend a line to predict `[extra = beyond (L)]`; convention — agreed naming rule `[con- = together; ven = come (L)]`; irreversible — cannot be undone `[in- = not; re- = back; vers = turn (L)]`; release — to give off `[re- = back; lax = loosen (L)]`; absorb — to take in `[ab- = away; sorb = suck (L)]`

**Tier 3:** combustion — burning of a fuel in oxygen `[com- = up; bust = burn (L)]`; fuel — a store of chemical energy `[—]`; oxidation — element reacting with oxygen `[oxy = sharp/acid (Gk); -ation = process]`; oxide — a compound of a metal with oxygen `[-ide = binary compound]`; thermal decomposition — heat breaking one reactant into products `[therm = heat (Gk); de- = apart; compos = put (L)]`; decompose — break a substance down `[de- = apart; compos = put together (L)]`; displacement — reactive element takes another's place `[dis- = apart; plac = place (L)]`; carbonate — salt containing carbon and oxygen `[carbo = coal (L); -ate = salt]`; limewater — solution used to test for carbon dioxide `[—]`; combustion triangle — fuel, oxygen and heat needed for fire `[—]`; ignition — the starting of burning `[igni = fire (L)]`; rust — iron oxide from oxidation `[—]`; reactivity — how readily an element reacts `[re- = again (L)]`

### C4.3 Conservation of mass
**Tier 2:** conserve — to keep the total unchanged `[con- = together; serv = keep (L)]`; conservation — keeping a quantity constant `[con- = together; serv = keep (L)]`; rearrange — reorder the atoms `[re- = again; arrange (Fr)]`; recombine — join together again differently `[re- = again; com- = together (L)]`; balance — make both sides equal `[bi- = two; lanx = plate (L)]`; calculate — work out a value `[calcul = pebble (L)]`; total — the whole amount added up `[tot = all (L)]`; equal — the same in amount `[aequa = equal (L)]`; ratio — proportional relationship `[rat = reckon (L)]`; anomalous — not fitting the pattern `[an- = not; homalos = even (Gk)]`; predict — to forecast a value `[prae- = before; dict = say (L)]`; escape — to get away (a gas leaving) `[ex- = out; cappa = cape (L)]`

**Tier 3:** law of conservation of mass — mass of reactants equals mass of products `[—]`; mass — the amount of matter `[massa = lump (L)]`; balanced equation — equation with equal atoms each side `[—]`; coefficient — big number in front of a formula `[co- = together; effic = make (L)]`; bubble diagram — drawing of atoms as circles `[—]`; precipitate — solid formed in the reaction `[prae- = before; cipit = headlong (L)]`; digital balance — instrument for measuring mass `[—]`; atom — smallest particle of an element `[a- = not; tom = cut (Gk)]`

### C4.4 Acids, alkalis and pH
**Tier 2:** identify — to recognise/tell apart `[ident = same (L)]`; measure — to find a quantity `[mensur = measure (L)]`; dissolve — to mix into a solution `[dis- = apart; solv = loosen (L)]`; cancel — to counteract/neutralise `[cancell = lattice (L)]`; increase — to raise/make greater `[in- = into; cresc = grow (L)]`; differentiate — to distinguish between `[differ = carry apart (L)]`; range — a span of values `[—]`; distinguish — to tell one from another `[distingu = mark off (L)]`; scale — a graded measuring range `[scala = ladder (L)]`

**Tier 3:** acid — chemical with pH below 7 `[acid = sour (L)]`; alkali — chemical with pH above 7 `[al-qali = ashes (Ar)]`; alkaline — having the properties of an alkali `[al-qali = ashes (Ar)]`; neutral — neither acid nor alkali, pH 7 `[neuter = neither (L)]`; pH scale — measure of acidity/alkalinity 0–14 `[p = potential; H = hydrogen]`; corrosive — able to burn/eat away material `[cor- = intensive; rod = gnaw (L)]`; indicator — chemical that changes colour with pH `[indic = point out (L)]`; universal indicator — dye giving the full pH colour range `[uni = one; vers = turn (L)]`; base — chemical that neutralises an acid `[bas = foundation (Gk)]`; soluble — able to dissolve in water `[solu = loosen (L)]`; insoluble — unable to dissolve in water `[in- = not; solu = loosen (L)]`; hydroxide — compound containing OH, often an alkali `[hydro = water (Gk); -ide = compound]`; hazard symbol — sign warning of danger `[—]`; pH probe — electronic sensor for measuring pH `[—]`

### C4.5 Reactions of acids with metals and alkalis
**Tier 2:** neutralise — to make neutral (cancel an acid) `[neuter = neither (L)]`; predict — to state the likely products `[prae- = before; dict = say (L)]`; produce — to form as a product `[pro- = forth; duc = lead (L)]`; substitute — swap a chemical into an equation `[sub- = under; stitu = set (L)]`; represent — to write as an equation `[re- = again; present = show (L)]`; subset — a smaller group within a family `[sub- = under; set]`; correspond — to match/relate to `[con- = together; respond = answer (L)]`; observe — to watch and note `[ob- = towards; serv = watch (L)]`

**Tier 3:** neutralisation — reaction of acid with alkali giving salt and water `[neuter = neither (L); -ation = process]`; salt — product of an acid reaction `[sal = salt (L)]`; chloride — salt from hydrochloric acid `[chlor = green (Gk); -ide = compound]`; sulfate — salt from sulfuric acid `[-ate = salt with oxygen]`; nitrate — salt from nitric acid `[nitr = soda (Gk); -ate = salt]`; hydroxide — the OH-containing part of an alkali `[hydro = water (Gk)]`; hydrogen — gas produced when metals react with acid `[hydro = water; gen = producing (Gk)]`; squeaky pop test — burning splint test for hydrogen `[—]`; dilute — acid weakened with water `[di- = apart; lu = wash (L)]`; concentration — amount of solute in a solution `[con- = together; centr = centre (L)]`

## C5 Energy changes

### C5.1 Changes of state
**Tier 2:** transfer — to move energy from place to place `[trans- = across; fer = carry (L)]`; supply — to provide energy `[sub- = up; ple = fill (L)]`; overcome — to defeat/get past (forces) `[—]`; identify — to recognise a feature `[ident = same (L)]`; increase — to become greater `[in- = into; cresc = grow (L)]`; remain — to stay the same `[re- = back; man = stay (L)]`; represent — to stand for on a graph `[re- = again; present = show (L)]`; model — a physical representation `[modul = measure (L)]`; sequence — an ordered series of steps `[sequ = follow (L)]`

**Tier 3:** change of state — melting, boiling, freezing, condensing `[—]`; melting — solid turning to liquid `[—]`; melting point — temperature of solid-to-liquid change `[—]`; boiling — liquid turning to gas `[—]`; boiling point — temperature of liquid-to-gas change `[—]`; evaporation — liquid turning to gas `[e- = out; vapor = steam (L)]`; freezing — liquid turning to solid `[—]`; condensing — gas turning to liquid `[con- = together; dens = thick (L)]`; particle — a tiny piece of matter `[particula = small part (L)]`; heating curve — graph of temperature against time `[—]`; thermal store — the heat energy of a substance `[therm = heat (Gk)]`; state of matter — solid, liquid or gas `[—]`

### C5.2 Endothermic and exothermic reactions
**Tier 2:** categorise — to sort into groups `[categor = accuse/predicate (Gk)]`; transfer — to move energy in or out `[trans- = across; fer = carry (L)]`; release — to give out energy `[re- = back; lax = loosen (L)]`; absorb — to take in energy `[ab- = away; sorb = suck (L)]`; surroundings — everything around the reaction `[—]`; qualitative — described, not measured with numbers `[qual = of what kind (L)]`; quantitative — described using numbers `[quant = how much (L)]`; measure — to find the temperature change `[mensur = measure (L)]`; calculate — to work out mean and range `[calcul = pebble (L)]`

**Tier 3:** exothermic — reaction releasing heat to surroundings `[exo- = out (Gk); therm = heat]`; endothermic — reaction taking in heat from surroundings `[endo- = in (Gk); therm = heat]`; thermal decomposition — heat breaking down a reactant (endothermic) `[therm = heat (Gk); de- = apart; compos = put (L)]`; neutralisation — acid–alkali reaction (exothermic) `[neuter = neither (L)]`; thermometer — instrument measuring temperature `[therm = heat; meter = measure (Gk)]`; mean — the average of a data set `[—]`; anhydrous — containing no water `[an- = without; hydro = water (Gk)]`

## Chemistry — Year 3

## C6 The Periodic Table

### C6.1 Properties of metals and non-metals
**Tier 2:** property — a characteristic of a material `[proprius = one's own (L)]`; classify — sort into groups by shared traits `[classis = group/class (L)]`; conductor — thing that transfers energy well `[con- = with; ducere = to lead (L)]`; insulator — thing that resists energy transfer `[insula = island (L)]`; react — undergo a chemical change `[re- = back/again; agere = to act (L)]`; produce — form as an output `[pro- = forth; ducere = to lead (L)]`; general (equation) — applying to all cases `[genus = kind (L)]`; transfer — move energy from one place to another `[trans- = across; ferre = to carry (L)]`
**Tier 3:** metal — shiny, conductive, malleable element `[metallon = mine/metal (Gk)]`; non-metal — element lacking metallic properties `[non- = not (L)]`; malleable — can be hammered into shape `[malleus = hammer (L)]`; brittle — breaks/snaps easily `[—]`; thermal — relating to heat `[thermē = heat (Gk)]`; oxide — compound of an element with oxygen `[oxy- = sharp/acid (Gk); -ide = compound]`; basic — having properties of a base `[—]`; acidic — having properties of an acid `[acidus = sour (L)]`; magnetic — attracted by a magnet `[Magnēsia = place-name (Gk)]`; reactive — readily undergoes reactions `[re- = back; agere = to act (L)]`; element — pure substance of one atom type `[elementum = first principle (L)]`; pH — scale of acidity/alkalinity `[—]`

### C6.2 Groups, periods, metals and non-metals
**Tier 2:** group — a column of similar elements `[—]`; period — a row across the table `[peri- = around; hodos = way (Gk)]`; predict — say what will happen in advance `[prae- = before; dicere = to say (L)]`; order — arrange in a sequence `[ordo = row/rank (L)]`; arrange — put into a deliberate order `[—]`; trend — a pattern or direction of change `[—]`; property — characteristic of a material `[proprius = one's own (L)]`; classify — sort by shared characteristics `[classis = class (L)]`; evidence — data supporting a claim `[e- = out; videre = to see (L)]`; react — undergo chemical change `[re- = back; agere = to act (L)]`
**Tier 3:** Periodic Table — chart of elements by properties `[peri-/hodos = recurring (Gk)]`; atomic mass — mass of an atom `[atomos = uncuttable (Gk)]`; atomic number — number of protons in an atom `[atomos = uncuttable (Gk)]`; reactivity — tendency to react `[re- + -ivity = state of]`; electron — negative subatomic particle `[ēlektron = amber (Gk)]`; shell — energy level holding electrons `[—]`; nucleus — dense centre of an atom `[nucleus = little nut (L)]`; electronic structure — arrangement of electrons `[ēlektron = amber (Gk)]`; alkali metal — reactive Group 1 metal `[al-qaliy = the ashes (Ar)]`; halogen — reactive Group 7 element `[hals = salt; -gen = producing (Gk)]`; noble gas — inert Group 0 element `[—]`; inert — chemically unreactive `[iners = idle/inactive (L)]`; displacement (reaction) — more reactive element replaces a less reactive one `[dis- = apart; placere = to place (L)]`; hydroxide — compound containing OH group `[hydor = water; oxy = acid (Gk)]`; vapour — gaseous form of a substance `[vapor = steam (L)]`

## C7 Materials

### C7.1 Metal reactivity
**Tier 2:** series — an ordered sequence `[serere = to join/link (L)]`; predict — foretell an outcome `[prae- = before; dicere = to say (L)]`; observation — something seen/heard/noted `[ob- = towards; servare = to watch (L)]`; react — undergo chemical change `[re- = back; agere = to act (L)]`; product — output of a reaction `[pro- = forth; ducere = to lead (L)]`; reactant — a starting substance in a reaction `[re- + agere = to act (L)]`; data — recorded results `[datum = given thing (L)]`; vigorous — energetic/forceful `[vigor = liveliness (L)]`; equation — statement of equal sides `[aequus = equal (L)]`
**Tier 3:** reactivity — tendency to react `[re- + -ivity = state of]`; reactivity series — metals ranked by reactivity `[serere = to link (L)]`; displacement (reaction) — reactive element replaces less reactive `[dis- = apart; placere = to place (L)]`; compound — substance of bonded elements `[com- = together; ponere = to put (L)]`; salt — ionic product of acid + metal `[sal = salt (L)]`; hydrogen — lightest gas, formed with acids `[hydor = water; -gen = producing (Gk)]`; corrosion (implied via reactivity) — gradual chemical wearing away `[cor-/rodere = to gnaw (L)]`; dilute — weakened by adding water `[di- = apart; luere = to wash (L)]`

### C7.2 Metal extraction with carbon
**Tier 2:** extract — remove/draw out `[ex- = out; trahere = to pull (L)]`; extraction — the process of drawing out `[ex- = out; trahere = to pull (L)]`; process — a sequence of steps `[pro- = forth; cedere = to go (L)]`; reduce — lower or remove (here, remove oxygen) `[re- = back; ducere = to lead (L)]`; reduction — a lowering; removal of oxygen `[re- = back; ducere = to lead (L)]`; compound — combination of bonded elements `[com- = together; ponere = to put (L)]`; model — a simplified representation `[modulus = small measure (L)]`
**Tier 3:** ore — rock containing extractable metal `[—]`; oxidation — reaction adding oxygen `[oxy- = acid (Gk); -ation = process]`; reduction (chemical) — removal of oxygen from a compound `[re- = back; ducere = to lead (L)]`; furnace — very hot chamber for extraction `[fornax = oven (L)]`; ingot — a cast block of metal `[—]`; carbon — non-metal element used to reduce ores `[carbo = coal (L)]`; magnetite — an iron ore `[Magnēsia (Gk); -ite = mineral]`; haematite — an iron-oxide ore `[haima = blood (Gk); -ite = mineral]`; electrolysis — splitting a compound with electricity `[ēlektron = amber; lysis = loosening (Gk)]`

### C7.3 Ceramics, polymers and composites
**Tier 2:** structure — how parts are arranged `[struere = to build (L)]`; property — characteristic of a material `[proprius = one's own (L)]`; combine — join together `[com- = together; bini = two-by-two (L)]`; combination — a joining together `[com- = together; bini = pairs (L)]`; compress — squeeze/press together `[com- = together; premere = to press (L)]`; magnify — make appear larger `[magnus = great; facere = to make (L)]`; unit — a single repeating part `[unus = one (L)]`; network — an interconnected structure `[—]`; support — hold up/bear weight `[sub- = under; portare = to carry (L)]`; evaporate — turn liquid into vapour `[e- = out; vapor = steam (L)]`
**Tier 3:** ceramic — hard material made by firing clay `[keramos = potter's clay (Gk)]`; polymer — long chain of repeating units `[poly- = many; -mer = part (Gk)]`; monomer — single repeating unit of a polymer `[mono- = one; -mer = part (Gk)]`; composite — material combining two others `[com- = together; ponere = to put (L)]`; molecule — group of bonded atoms `[molecula = small mass (L)]`; clay — fine earthy material fired into ceramic `[—]`; brittle — snaps rather than bends `[—]`; insulator — poor conductor of heat/electricity `[insula = island (L)]`; reinforced (concrete) — strengthened with another material `[re- = again; in- + fortis = strong (L)]`; rebar — reinforcing steel bar `[re- = again; + bar]`; fibreglass — glass fibres in resin `[fibra = fibre (L)]`; resin — sticky binding substance `[resina = resin (L)]`; bonded — chemically joined `[—]`; unreactive — does not readily react `[un- = not; re- + agere (L)]`

## C8 The Earth and its atmosphere

### C8.1 Composition and structure of the Earth
**Tier 2:** composition — what something is made of `[com- = together; ponere = to put (L)]`; structure — how parts are arranged `[struere = to build (L)]`; layer — one level of material `[—]`; uniform — the same throughout `[uni- = one; forma = shape (L)]`; consistent — unchanging/steady throughout `[com- = together; sistere = to stand (L)]`; generate — produce/bring into being `[genus = origin (L)]`; pressure — force per unit area `[premere = to press (L)]`; exploit — make use of a resource `[explicare = to unfold (L)]`; resource — a usable material supply `[re- + surgere = to rise again (L)]`
**Tier 3:** crust — solid outer layer of the Earth `[crusta = shell/rind (L)]`; mantle — thick semi-molten middle layer `[mantellum = cloak (L)]`; core — dense metallic centre `[—]`; atmosphere — layer of gases around Earth `[atmos = vapour; sphaira = ball (Gk)]`; tectonic plate — a moving slab of crust `[tektōn = builder (Gk)]`; molten — melted into liquid `[—]`; semi-molten — partly melted `[semi- = half (L)]`; density / denser — mass per unit volume `[densus = thick (L)]`; magnetic field — region of magnetic force `[Magnēsia (Gk)]`; gravitational — relating to gravity's pull `[gravis = heavy (L)]`; earthquake — ground shaking from plate movement `[—]`; volcanic — relating to erupting magma `[Vulcanus = Roman fire-god]`

### C8.2 The rock cycle
**Tier 2:** cycle — a repeating sequence `[kyklos = circle (Gk)]`; classify — sort by category `[classis = class (L)]`; form / formation — come into being; the making of `[forma = shape (L)]`; process — a sequence of steps `[pro- = forth; cedere = to go (L)]`; texture — the feel/appearance of a surface `[texere = to weave (L)]`; structure — arrangement of parts `[struere = to build (L)]`; dynamic — constantly changing `[dynamis = power (Gk)]`; deposit — lay down material `[de- = down; ponere = to put (L)]`; accumulate — build up over time `[ad- = to; cumulus = heap (L)]`
**Tier 3:** igneous — rock formed from cooled molten rock `[ignis = fire (L)]`; metamorphic — rock changed by heat and pressure `[meta- = change; morphē = form (Gk)]`; metamorphism — the process of such change `[meta- = change; morphē = form (Gk)]`; sedimentary — rock formed from compressed sediment `[sedere = to settle (L)]`; sediment — small fragments of weathered rock `[sedere = to settle (L)]`; sedimentation — settling of sediment `[sedere = to settle (L)]`; weathering — physical breakdown of rock `[—]`; transportation — carrying of sediment away `[trans- = across; portare = to carry (L)]`; compaction — pressing sediment into rock `[com- = together; pangere = to fasten (L)]`; crystal — regular solid grain in rock `[krystallos = ice (Gk)]`; coarse-grained — having large grains `[—]`; fine-grained — having small grains `[—]`; impermeable — water cannot pass through `[im- = not; per- = through; meare = to pass (L)]`; porous — full of pores; lets water through `[poros = passage (Gk)]`; interlocking — grains fitted tightly together `[inter- = between (L)]`; mineral — naturally occurring solid compound `[minera = ore (L)]`; fossil — preserved remains in rock `[fossilis = dug up (L)]`; geological — relating to Earth's rocks `[geo- = earth; logos = study (Gk)]`; lava — molten rock at the surface `[lavare = to wash (L)]`

### C8.3 Composition of the atmosphere
**Tier 2:** composition — what a mixture is made of `[com- = together; ponere = to put (L)]`; resource — usable material supply `[re- + surgere = to rise again (L)]`; proportion — a relative amount/share `[pro- = for; portio = share (L)]`; percentage — an amount per hundred `[per centum = by the hundred (L)]`; abundant — present in large quantity `[abundare = to overflow (L)]`; regulate — control/keep in balance `[regula = rule (L)]`; essential — absolutely necessary `[esse = to be (L)]`; vary — change/differ `[varius = diverse (L)]`
**Tier 3:** atmosphere — layer of gases around Earth `[atmos = vapour; sphaira = ball (Gk)]`; mixture — substances physically combined, not bonded `[miscere = to mix (L)]`; molecule — group of bonded atoms `[molecula = small mass (L)]`; chemical formula — symbols showing atoms in a substance `[—]`; inert — chemically unreactive `[iners = idle (L)]`; noble gas — inert Group 0 gas `[—]`; combustion — burning; reaction with oxygen `[comburere = to burn up (L)]`; respiration — cellular energy release using oxygen `[re- = again; spirare = to breathe (L)]`; photosynthesis — plants making glucose using light `[photo- = light; synthesis = putting together (Gk)]`; aerobic — requiring oxygen `[aēr = air; bios = life (Gk)]`; nitrogen — main atmospheric gas (78%) `[nitron = soda; -gen = producing (Gk)]`; oxygen — reactive gas (21%) `[oxy- = acid; -gen = producing (Gk)]`; argon — inert atmospheric gas `[argos = inactive (Gk)]`; protein — biological molecule containing nitrogen `[prōtos = first (Gk)]`; amino acid — building block of proteins `[—]`; water vapour — gaseous water `[vapor = steam (L)]`

### C8.4 The carbon cycle
**Tier 2:** cycle — a repeating sequence `[kyklos = circle (Gk)]`; process — a sequence of steps `[pro- = forth; cedere = to go (L)]`; balance — a stable equilibrium `[bilanx = two-panned scale (L)]`; absorb — take in `[ab- = from; sorbere = to suck (L)]`; emit — give off/release `[e- = out; mittere = to send (L)]`; release — let out/set free `[re- + laxare = to loosen (L)]`; store / pool — a place where something accumulates `[—]`; increase — become greater `[in- + crescere = to grow (L)]`; rate — speed at which something happens `[rata = fixed amount (L)]`; activity — action/process `[agere = to act (L)]`
**Tier 3:** carbon cycle — movement of carbon through stores `[kyklos = circle (Gk)]`; greenhouse effect — warming by heat-trapping gases `[—]`; greenhouse gas — gas that absorbs radiated heat `[—]`; global warming — rise in average Earth temperature `[globus = ball (L)]`; climate change — long-term shifts in weather patterns `[klima = region/slope (Gk)]`; carbon dioxide — greenhouse gas, CO2 `[di- = two; oxy = acid (Gk)]`; methane — carbon-hydrogen greenhouse gas `[—]`; combustion — burning of fuels `[comburere = to burn up (L)]`; respiration — energy release giving off CO2 `[re- = again; spirare = to breathe (L)]`; photosynthesis — carbon fixing by plants `[photo- = light; synthesis = putting together (Gk)]`; fossil fuel — carbon-rich fuel from ancient organisms `[fossilis = dug up (L)]`; deforestation — cutting down of forests `[de- = removal; forest; -ation = process]`; glucose — sugar made in photosynthesis `[gleukos = sweet (Gk)]`; exothermic — releasing heat `[exo- = out; thermē = heat (Gk)]`; endothermic — absorbing heat `[endo- = within; thermē = heat (Gk)]`; carbon store / carbon pool — reservoir of carbon `[—]`; non-renewable — cannot be replaced quickly `[non- = not; re- + novus = new (L)]`

### C8.5 Recycling
**Tier 2:** recycle — process material for reuse `[re- = again; kyklos = circle (Gk)]`; reuse — use again `[re- = again (L)]`; reduce — use less `[re- = back; ducere = to lead (L)]`; resource — usable material supply `[re- + surgere = to rise again (L)]`; extract — draw out (a metal) `[ex- = out; trahere = to pull (L)]`; beneficial — producing good effects `[bene = well; facere = to do (L)]`; sustainable — able to be maintained long-term `[sub- = up; tenere = to hold (L)]`; environment — the surroundings `[environ = around (Fr)]`; source — origin of a material `[surgere = to rise (L)]`; advantage — a beneficial factor `[ab-ante = in front (L)]`
**Tier 3:** recycling — reprocessing materials into new products `[re- = again; kyklos = circle (Gk)]`; renewable — can be replenished naturally `[re- = again; novus = new (L)]`; finite — limited in amount `[finis = end (L)]`; ore — metal-bearing rock `[—]`; mining — digging resources from the ground `[—]`; fossil fuel — carbon fuel from ancient organisms `[fossilis = dug up (L)]`; polymer — long-chain molecule (e.g. from starch) `[poly- = many; -mer = part (Gk)]`; sustainable development — meeting needs without depleting resources `[sub- = up; tenere = to hold (L)]`; habitat — natural home of organisms `[habitare = to dwell (L)]`; non-renewable — cannot be regrown/replaced `[non- = not; re- + novus = new (L)]`


---

# PHYSICS

## Physics — Year 1

## P1 Energy

### P1.1 Fuels and energy stores
**Tier 2:** store — where energy is held before use `[—]`; transfer — movement of energy between places `[trans- = across (Lat)]`; release — let stored energy escape `[—]`; measure — find a quantity as a number `[—]`; represent — show something using a diagram `[re- = again (Lat)]`; substance — a material that can burn `[sub-stare = stand under (Lat)]`; convert — change from one unit to another `[con-vertere = turn with (Lat)]`; quantify — express as a number `[quantus = how much (Lat)]`; approximation — a rough, simplified value `[proxim- = nearest (Lat)]`; conserve — keep the total amount unchanged `[con-servare = keep together (Lat)]`
**Tier 3:** fuel — substance that releases energy when burned `[—]`; chemical energy store — energy held in a fuel/battery `[chem- = alchemy (Gk/Arabic)]`; thermal energy store — energy linked to temperature `[therm- = heat (Gk)]`; joule — the unit of energy `[—]`; kilojoule — one thousand joules `[kilo- = thousand (Gk)]`; conservation of energy — energy cannot be created or destroyed `[—]`; efficient — transferring energy without waste `[—]`

### P1.2 Energy stores and transfers
**Tier 2:** transfer — energy moving between stores `[trans- = across (Lat)]`; store — reservoir holding energy `[—]`; convert — turn one form into another `[con-vertere = turn with (Lat)]`; direction — the line along which motion occurs `[—]`; constant — staying the same `[con-stare = stand firm (Lat)]`; ubiquitous — present everywhere `[ubique = everywhere (Lat)]`; alter — to change `[alter = other (Lat)]`; overall — total, taken as a whole `[—]`
**Tier 3:** kinetic energy store — energy of a moving object `[kine- = move (Gk)]`; gravitational energy store — energy of a raised object `[gravis = heavy (Lat)]`; elastic energy store — energy in a stretched/squashed object `[—]`; stationary — not moving `[stare = to stand (Lat)]`; transfer by a force — energy moved by a push/pull `[—]`; transfer by electric current — energy moved by a current `[—]`; transfer by waves — energy carried by sound/light `[—]`; radiate — spread outward from a source `[radius = ray (Lat)]`; extension — increase in length when stretched `[ex-tendere = stretch out (Lat)]`; compression — reduction in size when squashed `[com-premere = press together (Lat)]`

### P1.3 Power
**Tier 2:** calculate — work out a value mathematically `[calc- = pebble/count (Lat)]`; equation — a maths statement linking quantities `[aequus = equal (Lat)]`; convert — change between units `[con-vertere = turn with (Lat)]`; predict — say what will happen beforehand `[pre-dicere = say before (Lat)]`; interchangeably — used as if the same `[inter- = between (Lat)]`; conflate — wrongly merge two ideas `[con-flare = blow together (Lat)]`; manipulation — handling values mathematically `[manus = hand (Lat)]`; quantity — an amount that can be measured `[quantus = how much (Lat)]`; symbol — a letter standing for a quantity `[symbolon = token (Gk)]`
**Tier 3:** power — energy transferred each second `[—]`; watt — the unit of power `[—]`; kilowatt — one thousand watts `[kilo- = thousand (Gk)]`; joules per second — rate of energy transfer `[—]`; energy transferred — energy moved between stores `[—]`

### P1.4 Energy resources
**Tier 2:** resource — a supply we draw energy from `[re-surgere = rise again (Lat)]`; generate — to produce (electricity) `[gener- = bring forth (Lat)]`; source — the origin of something `[surgere = to rise (Lat)]`; obtain — to get or acquire `[ob-tenere = hold onto (Lat)]`; distinguish — tell apart from something else `[stinguere = separate (Lat)]`; categorise — sort into groups `[kategoria = class (Gk)]`; process — a sequence of changes `[pro-cedere = go forward (Lat)]`; contribute — add to a larger effect `[con-tribuere = grant with (Lat)]`
**Tier 3:** energy resource — source used to supply energy `[re-surgere = rise again (Lat)]`; fossil fuel — fuel from ancient organisms `[fossilis = dug up (Lat)]`; turbine — spinning blade driven by a fluid `[turbo = whirl (Lat)]`; generator — machine making electric current from motion `[gener- = produce (Lat)]`; hydroelectric — electricity from moving water `[hydro- = water (Gk)]`; National Grid — cable network transferring electricity `[—]`; renewable — replaced as fast as used `[re-novare = make new again (Lat)]`; non-renewable — cannot be replaced in time `[non- = not (Lat)]`; biofuel — fuel from biological material `[bio- = life (Gk)]`; carbon dioxide — gas released when fuels burn `[di- = two (Gk); oxy- = sharp (Gk)]`; compacted — pressed tightly together `[com-pangere = pack together (Lat)]`; organism — a living thing `[organon = tool/organ (Gk)]`

## P2 Speed

### P2.1 Speed
**Tier 2:** calculate — find a value by maths `[calc- = pebble/count (Lat)]`; measure — determine a quantity `[—]`; equation — maths statement linking quantities `[aequus = equal (Lat)]`; convert — change between units `[con-vertere = turn with (Lat)]`; variable — a factor that can change `[variare = to change (Lat)]`; procedure — a set method of doing something `[pro-cedere = go forward (Lat)]`; inquiry — an investigative question `[in-quaerere = seek into (Lat)]`; predict — state an expected outcome `[pre-dicere = say before (Lat)]`; controlled — kept the same on purpose `[contra-rotulus = check-list (Lat)]`; annotate — add explanatory labels `[ad-notare = mark to (Lat)]`
**Tier 3:** speed — distance travelled per unit time `[—]`; distance — how far apart two points are `[di-stare = stand apart (Lat)]`; metres per second (m/s) — the scientific unit of speed `[—]`; stationary — not moving `[stare = to stand (Lat)]`; line of inquiry — the cause-effect question tested `[—]`; independent variable — the factor deliberately changed `[—]`; dependent variable — the factor that responds `[de-pendere = hang from (Lat)]`; controlled variable — a factor kept constant `[—]`; trundle wheel — tool for measuring long distances `[—]`; ramp — an inclined surface `[—]`

### P2.2 Distance–time graphs
**Tier 2:** graph — a visual plot of data `[graphein = to write (Gk)]`; interpret — read meaning from data `[interpretari = explain (Lat)]`; plot — mark data points on axes `[—]`; compare — examine for similarities `[com-par = equal with (Lat)]`; trend — the general pattern in data `[—]`; represent — show by a line or symbol `[re- = again (Lat)]`; calculate — work out mathematically `[calc- = count (Lat)]`; axis — a reference line on a graph `[axis = axle (Lat)]`; scale — the spacing of values on an axis `[scala = ladder (Lat)]`
**Tier 3:** distance–time graph — graph of distance against time `[—]`; gradient — steepness of a line `[gradi = to step (Lat)]`; slope — the incline of a graph line `[—]`; horizontal — parallel to the ground `[horizon = boundary (Gk)]`; x-axis — the horizontal axis `[—]`; y-axis — the vertical axis `[—]`; line of best fit — line through plotted points `[—]`; stationary — shown by a horizontal line `[stare = to stand (Lat)]`

### P2.3 Relative motion
**Tier 2:** relative — compared with something else `[re-ferre = carry back (Lat)]`; compare — set two things side by side `[com-par = equal with (Lat)]`; motion — the state of moving `[movere = to move (Lat)]`; opposite — facing the other way `[ob-ponere = set against (Lat)]`; absolute — actual, not compared `[ab-solvere = set free (Lat)]`
**Tier 3:** relative motion — motion of one object compared with another `[re-ferre = carry back (Lat)]`; relative speed — combined/difference speed felt between objects `[—]`; one-dimensional — along a single line `[—]`; overtake — to pass a slower object `[—]`

## P3 Forces

### P3.1 Basic forces and diagrams
**Tier 2:** force — a push or pull on an object `[fortis = strong (Lat)]`; direction — the way a force acts `[—]`; represent — show a force with an arrow `[re- = again (Lat)]`; identify — name and pick out `[idem = the same (Lat)]`; magnitude — the size of a force `[magnus = large (Lat)]`; effect — result produced `[ex-facere = bring about (Lat)]`; perpendicular — at right angles `[per-pendere = plumb-line (Lat)]`
**Tier 3:** friction — force opposing rubbing surfaces `[fricare = to rub (Lat)]`; gravity — pull towards the Earth `[gravis = heavy (Lat)]`; magnetism — force between magnets `[magnes = lodestone (Gk)]`; weight — force of gravity on a mass `[—]`; newton (N) — the unit of force `[—]`; newtonmeter — device to measure force `[—]`; force arrow — arrow showing force direction/size `[—]`; centre of mass — point where an object's mass acts `[—]`

### P3.2 Naming and categorising forces
**Tier 2:** categorise — sort forces into groups `[kategoria = class (Gk)]`; interact — act on one another `[inter-agere = act between (Lat)]`; oppose — act against `[ob-ponere = set against (Lat)]`; resist — push back against motion `[re-sistere = stand back (Lat)]`; attract — pull together `[ad-trahere = draw toward (Lat)]`; repel — push apart `[re-pellere = drive back (Lat)]`; orient — arrange in a direction `[oriens = rising (Lat)]`; distinguish — tell apart `[stinguere = separate (Lat)]`
**Tier 3:** applied force — a person's push or pull `[ap-plicare = fold onto (Lat)]`; thrust — push force from an engine `[—]`; friction — force from rubbing surfaces `[fricare = to rub (Lat)]`; air resistance — friction from air particles `[re-sistere = stand back (Lat)]`; water resistance — friction from water particles `[—]`; upthrust — upward push from a fluid `[—]`; lift — upward force on flying objects `[—]`; normal contact force — force perpendicular to a surface `[norma = right-angle rule (Lat)]`; tension — pulling force in a stretched object `[tendere = to stretch (Lat)]`; magnetism — non-contact magnetic force `[magnes = lodestone (Gk)]`; interaction — paired forces between two objects `[inter-agere = act between (Lat)]`; contact force — force needing objects to touch `[—]`; non-contact force — force acting at a distance `[—]`; streamlined — shaped to reduce resistance `[—]`; poles — the ends of a magnet `[polos = pivot/axis (Gk)]`

### P3.3 Stretching and squashing forces
**Tier 2:** convert — change millimetres to metres `[con-vertere = turn with (Lat)]`; property — a characteristic of a material `[proprius = one's own (Lat)]`; original — the starting state `[origo = beginning (Lat)]`; opposite — in the reverse direction `[ob-ponere = set against (Lat)]`; define — state the exact meaning `[de-finire = set limits (Lat)]`
**Tier 3:** compression — shortening when squashed `[com-premere = press together (Lat)]`; extension — lengthening when stretched `[ex-tendere = stretch out (Lat)]`; deformation — change of shape by a force `[de-forma = un-shape (Lat)]`; elastic — returns to original shape `[elastikos = springy (Gk)]`; inelastic — stays deformed `[in- = not (Lat)]`; millimetre — one thousandth of a metre `[milli- = thousand (Lat)]`; elasticity — the property of springing back `[elastikos = springy (Gk)]`

### P3.4 Hooke's law and work done
**Tier 2:** relationship — a link between variables `[—]`; linear — forming a straight line `[linea = line (Lat)]`; proportional — increasing at a set ratio `[pro-portio = for share (Lat)]`; variable — a quantity that changes `[variare = to change (Lat)]`; component — a part of a whole skill `[com-ponere = put together (Lat)]`; derive — obtain an equation from reasoning `[de-rivare = draw off (Lat)]`; qualitative — described without numbers `[qualis = of what kind (Lat)]`; error — a mistake in measurement `[errare = to wander (Lat)]`; systematic — following a consistent pattern `[systema = ordered whole (Gk)]`
**Tier 3:** Hooke's law — force equals spring constant times extension `[—]`; spring constant — force needed to extend a spring 1 m `[—]`; extension — increase in a spring's length `[ex-tendere = stretch out (Lat)]`; newtons per metre (N/m) — the unit of spring constant `[—]`; linear relationship — set increase gives set increase `[linea = line (Lat)]`; work done — energy transferred by a force `[—]`; elastic energy store — energy in a stretched spring `[elastikos = springy (Gk)]`; gravitational energy store — energy of a raised mass `[gravis = heavy (Lat)]`; random error — unpredictable one-off mistake `[—]`; systematic error — a consistent offset in all results `[systema = ordered whole (Gk)]`

### P3.5 Moments and simple machines
**Tier 2:** calculate — work out the moment `[calc- = count (Lat)]`; equation — maths link between quantities `[aequus = equal (Lat)]`; proportion — the ratio between quantities `[pro-portio = for share (Lat)]`; balance — make two effects equal `[bi-lanx = two pans (Lat)]`; apply — put a force onto something `[ap-plicare = fold onto (Lat)]`; identify — pick out the pivot/load `[idem = the same (Lat)]`; anomalous — not fitting the trend `[an-homalos = not even (Gk)]`; qualitative — non-numerical description `[qualis = of what kind (Lat)]`
**Tier 3:** moment — a turning effect of a force `[—]`; pivot — the point about which turning occurs `[—]`; fulcrum — the support of a lever `[fulcire = to prop up (Lat)]`; newtonmetre (N m) — the unit of a moment `[—]`; clockwise — turning like clock hands `[—]`; anticlockwise — turning the opposite way `[anti- = against (Gk)]`; balanced moments — equal clockwise and anticlockwise moments `[—]`; lever — a bar turning about a pivot `[levare = to raise (Lat)]`; simple machine — device using moments to reduce force `[—]`; spanner — lever used to turn a nut `[—]`; load — the object being moved `[—]`; anomalous result — a value that does not fit the trend `[an-homalos = not even (Gk)]`

### P3.6 Balanced forces
**Tier 2:** balance — equal opposing forces `[bi-lanx = two pans (Lat)]`; resultant — the combined overall effect `[re-sultare = leap back (Lat)]`; cancel — forces removing each other's effect `[cancellare = cross out (Lat)]`; represent — show forces with arrows `[re- = again (Lat)]`; equal — the same in size `[aequus = level (Lat)]`; opposite — in the reverse direction `[ob-ponere = set against (Lat)]`; overall — taken as a whole `[—]`
**Tier 3:** balanced forces — equal, opposite forces with zero resultant `[—]`; resultant force — the overall force on an object `[re-sultare = leap back (Lat)]`; unbalanced force — a non-zero resultant `[—]`; equilibrium — state of balanced forces `[aequi-libra = equal balance (Lat)]`; constant speed — speed that does not change `[con-stare = stand firm (Lat)]`; normal reaction force — upward push from a surface `[norma = right-angle rule (Lat)]`

### P3.7 Forces and motion
**Tier 2:** resultant — the net combined force `[re-sultare = leap back (Lat)]`; quantitative — described using numbers `[quantus = how much (Lat)]`; qualitative — described without numbers `[qualis = of what kind (Lat)]`; motion — movement of an object `[movere = to move (Lat)]`; direction — the way an object moves `[—]`; scenario — a described situation `[scaena = stage (Lat)]`; reinforce — strengthen understanding `[re-fortis = strengthen again (Lat)]`
**Tier 3:** resultant force — overall force changing motion `[re-sultare = leap back (Lat)]`; accelerate — to speed up `[ad-celerare = hasten toward (Lat)]`; stationary — at rest, not moving `[stare = to stand (Lat)]`; constant speed — unchanging speed `[con-stare = stand firm (Lat)]`; arrowhead — symbol marking motion direction `[—]`

## Physics — Year 2

## P4 Pressure in fluids

### P4.1 Pressure in liquids
**Tier 2:** pressure — force per area on a surface `[—]`; classify — sort into named groups `[classis = class/group (L)]`; demonstration — a shown practical example `[de- = fully; monstrare = to show (L)]`; represent — stand for in a diagram `[re- = again; -present = present (L)]`; motion — the movement of particles `[movere = to move (L)]`; balance — equal and opposing forces `[bilanx = two-pan scale (L)]`; resultant — the overall single force `[re- = back; -sult = leap (L)]`; accelerate — speed up over time `[ad- = to; celer = swift (L)]`; volume — space an object occupies `[volumen = a roll (L)]`; distribute — spread out over an area `[dis- = apart; tribuere = to allot (L)]`
**Tier 3:** fluid — a substance that can flow `[fluere = to flow (L)]`; particle — tiny piece of matter `[particula = small part (L)]`; upthrust — upward force from a liquid `[up- + thrust]`; collision — particles hitting a surface `[collidere = to strike together (L)]`; submerge — put fully under water `[sub- = under; mergere = to dip (L)]`; density — particles packed in a volume `[densus = thick (L)]`

### P4.2 Atmospheric pressure
**Tier 2:** exert — apply a force outward `[ex- = out; serere = to put forth (L)]`; density — how tightly packed particles are `[densus = thick (L)]`; increase — become greater in amount `[in- = into; crescere = to grow (L)]`; decrease — become smaller in amount `[de- = down; crescere = to grow (L)]`; relationship — how two factors link `[relatus = carried back (L)]`; surface — the outer boundary of an object `[sur- = above; face (L)]`; factor — a thing that has an effect `[facere = to do/make (L)]`
**Tier 3:** atmosphere — layer of gases around Earth `[atmos = vapour (Gk); sphaira = ball (Gk)]`; atmospheric — relating to the atmosphere `[atmos = vapour (Gk); -ic = relating to]`; particle — tiny piece of matter `[particula = small part (L)]`; collision — particles striking a surface `[collidere = to strike together (L)]`; vacuum — a region with no particles `[vacuus = empty (L)]`

### P4.3 Pressure calculations
**Tier 2:** calculate — work out a numerical value `[calculus = pebble for counting (L)]`; area — the size of a flat surface `[area = open space (L)]`; equation — a mathematical statement of equality `[aequare = make equal (L)]`; distribute — spread the force across `[dis- = apart; tribuere = to allot (L)]`; qualitative — described without numbers `[qualis = of what kind (L)]`; unit — a standard measuring quantity `[unus = one (L)]`; value — a measured numerical amount `[valere = to be worth (L)]`; proportion — how force relates to area `[pro- = for; portio = share (L)]`
**Tier 3:** pressure — force spread over unit area `[premere = to press (L)]`; newton — the unit of force `[—]`

## P5 Sound

### P5.1 Types of wave
**Tier 2:** transfer — move energy from place to place `[trans- = across; ferre = to carry (L)]`; medium — the material a wave travels in `[medius = middle (L)]`; energy — capacity to do work `[en- = in; ergon = work (Gk)]`; material — the substance a wave passes through `[materia = matter (L)]`; reflect — bounce a wave back `[re- = back; flectere = to bend (L)]`; abstract — not concrete or observable `[abs- = away; trahere = to draw (L)]`; concrete — real and observable `[concretus = grown together (L)]`; motion — repeated movement `[movere = to move (L)]`; direction — the line along which a wave moves `[dirigere = to guide (L)]`; perpendicular — at 90° to a line `[perpendiculum = plumb line (L)]`; parallel — running alongside, never meeting `[para- = beside; allelos = one another (Gk)]`
**Tier 3:** wave — an oscillation that carries energy `[—]`; transverse — oscillating across the wave direction `[trans- = across; vertere = to turn (L)]`; longitudinal — oscillating along the wave direction `[longitudo = length (L)]`; oscillate — repeated back-and-forth motion `[oscillare = to swing (L)]`; oscillation — one repeated back-and-forth cycle `[oscillare = to swing (L)]`; vacuum — a region with no particles `[vacuus = empty (L)]`; peak — the highest point of a wave `[—]`; trough — the lowest point of a wave `[—]`; compression — particles squashed together `[com- = together; premere = to press (L)]`; rarefaction — particles spread apart `[rarus = thin/sparse (L); -faction = making]`; amplitude — maximum height of the oscillation `[amplus = large (L)]`; wavelength — distance between one peak and the next `[wave + length]`; particle — tiny piece of matter `[particula = small part (L)]`; electromagnetic — of the electric and magnetic field `[elektron = amber (Gk); magnes = magnet (Gk)]`; displacement — distance from the base line `[dis- = apart; placer = to place (L)]`

### P5.2 Sound waves
**Tier 2:** detect — sense that something is present `[detegere = to uncover (L)]`; transfer — move energy from place to place `[trans- = across; ferre = to carry (L)]`; frequency — how often an event repeats `[frequens = crowded/repeated (L)]`; medium — the material sound travels in `[medius = middle (L)]`; vibrate — an object oscillating rapidly `[vibrare = to shake (L)]`; vibration — a rapid back-and-forth movement `[vibrare = to shake (L)]`; interpret — work out the meaning of a signal `[interpretari = to explain (L)]`; temporal — relating to time `[tempus = time (L)]`; dynamic — moving and changing `[dynamis = power (Gk)]`; variable — a factor that changes `[variare = to change (L)]`; independent — the variable that is changed `[in- = not; dependere = to hang from (L)]`; dependent — the variable that is measured `[de- = down; pendere = to hang (L)]`; anomaly — a result that does not fit `[an- = not; homalos = even (Gk)]`; trend — a pattern in the data `[—]`; transmit — pass energy onward `[trans- = across; mittere = to send (L)]`
**Tier 3:** longitudinal — oscillating along the wave direction `[longitudo = length (L)]`; oscillate — repeated back-and-forth motion `[oscillare = to swing (L)]`; compression — particles squashed together `[com- = together; premere = to press (L)]`; rarefaction — particles spread apart `[rarus = thin (L); -faction = making]`; amplitude — the height of the wave `[amplus = large (L)]`; wavelength — distance between two peaks `[wave + length]`; pitch — how high or low a note is `[—]`; hertz — the unit of frequency `[—]`; ultrasound — sound above human hearing `[ultra = beyond (L); sound]`; infrasound — sound below human hearing `[infra = below (L); sound]`; cochlea — ear part making electrical signals `[kochlias = snail (Gk)]`; auditory — relating to hearing `[audire = to hear (L)]`; membrane — a thin stretched sheet `[membrana = skin (L)]`; diaphragm — a vibrating sheet in a device `[dia- = across; phragma = fence (Gk)]`; pinna — the outer flap of the ear `[pinna = feather/wing (L)]`

### P5.3 Microphones and ultrasound
**Tier 2:** detect — sense that a wave is present `[detegere = to uncover (L)]`; application — a practical use of an idea `[applicare = to attach (L)]`; technology — tools made from scientific ideas `[techne = craft (Gk); logos = study (Gk)]`; vibrate — oscillate rapidly `[vibrare = to shake (L)]`; mechanism — the working parts of a device `[mechane = machine (Gk)]`; emit — send out a wave `[e- = out; mittere = to send (L)]`; transfer — move energy from place to place `[trans- = across; ferre = to carry (L)]`; reflect — bounce a wave back `[re- = back; flectere = to bend (L)]`; convert — change from one form to another `[con- = with; vertere = to turn (L)]`
**Tier 3:** ultrasound — sound above human hearing `[ultra = beyond (L); sound]`; ultrasonic — using ultrasound `[ultra = beyond (L); sonus = sound (L)]`; echolocation — locating objects using echoes `[echo (Gk); locus = place (L)]`; echo — a reflected sound wave `[echo = returned sound (Gk)]`; diaphragm — vibrating sheet in a microphone `[dia- = across; phragma = fence (Gk)]`; membrane — a thin stretched sheet `[membrana = skin (L)]`; compression — the packed part of a sound wave `[com- = together; premere = to press (L)]`; rarefaction — the spread part of a sound wave `[rarus = thin (L); -faction = making]`; cochlea — ear part making signals `[kochlias = snail (Gk)]`

## P6 Light

### P6.1 Light and ray models
**Tier 2:** interact — act on one another `[inter- = between; agere = to act (L)]`; interaction — a mutual effect between things `[inter- = between; agere = to act (L)]`; transmit — pass energy through a material `[trans- = across; mittere = to send (L)]`; absorb — take in and stop energy `[ab- = away; sorbere = to suck in (L)]`; reflect — bounce a wave back `[re- = back; flectere = to bend (L)]`; source — where the energy comes from `[surgere = to rise (L)]`; detect — sense that light is present `[detegere = to uncover (L)]`; model — a simplified representation `[modulus = small measure (L)]`; density — how packed the particles are `[densus = thick (L)]`; qualitative — described without numbers `[qualis = of what kind (L)]`; magnitude — the size of a quantity `[magnus = great (L)]`
**Tier 3:** transverse — oscillating across the wave direction `[trans- = across; vertere = to turn (L)]`; oscillate — repeated back-and-forth motion `[oscillare = to swing (L)]`; electromagnetic — of the electric and magnetic field `[elektron = amber (Gk); magnes = magnet (Gk)]`; ray — a straight line showing light's path `[radius = spoke/beam (L)]`; vacuum — a region with no particles `[vacuus = empty (L)]`; amplitude — the height of the wave `[amplus = large (L)]`; wavelength — distance between two peaks `[wave + length]`; shadow — the dark area where light is blocked `[—]`

### P6.2 Interactions of light waves with materials
**Tier 2:** transmit — pass light through a material `[trans- = across; mittere = to send (L)]`; absorb — take in and stop light `[ab- = away; sorbere = to suck in (L)]`; reflect — bounce light back `[re- = back; flectere = to bend (L)]`; boundary — where two mediums meet `[bound + -ary]`; interface — the surface between two mediums `[inter- = between; facies = face (L)]`; emit — send out light `[e- = out; mittere = to send (L)]`; interaction — a mutual effect between wave and matter `[inter- = between; agere = to act (L)]`; angle — the amount of turn between lines `[angulus = corner (L)]`; direction — the line along which light moves `[dirigere = to guide (L)]`; perpendicular — at 90° to a surface `[perpendiculum = plumb line (L)]`
**Tier 3:** transparent — lets all light through `[trans- = through; parere = to appear (L)]`; translucent — lets some light through `[trans- = through; lucere = to shine (L)]`; opaque — lets no light through `[opacus = shaded/dark (L)]`; refract — light bending at a boundary `[re- = back; frangere = to break (L)]`; refraction — the bending of light at a boundary `[re- = back; frangere = to break (L)]`; normal — the line at 90° to a surface `[norma = carpenter's square (L)]`; ray — a straight line showing light's path `[radius = beam (L)]`; specular — mirror-like reflection `[speculum = mirror (L)]`

### P6.3 Mirrors, pinhole cameras and the eye
**Tier 2:** reflect — bounce light off a surface `[re- = back; flectere = to bend (L)]`; reflection — light bouncing off a surface `[re- = back; flectere = to bend (L)]`; transmit — pass light through `[trans- = across; mittere = to send (L)]`; absorb — take in and stop light `[ab- = away; sorbere = to suck in (L)]`; angle — the turn between two lines `[angulus = corner (L)]`; manipulate — control the direction of light `[manus = hand (L); plere = to fill (L)]`; magnify — make an image appear larger `[magnus = great (L); -fy = to make]`; generate — produce a signal `[generare = to produce (L)]`; detect — sense that light is present `[detegere = to uncover (L)]`; image — a picture formed by light `[imago = likeness (L)]`
**Tier 3:** incident — the ray coming in to a surface `[in- = onto; cadere = to fall (L)]`; incidence — angle of the incoming ray `[in- = onto; cadere = to fall (L)]`; normal — the line at 90° to the surface `[norma = square (L)]`; refract — light bending at a boundary `[re- = back; frangere = to break (L)]`; lens — a shaped glass that bends light `[lens = lentil (L)]`; convex — curving outward `[convexus = arched (L)]`; concave — curving inward `[concavus = hollow (L)]`; focus — the point where rays meet `[focus = hearth (L)]`; retina — the light-detecting layer of the eye `[rete = net (L)]`; iris — the coloured ring of the eye `[iris = rainbow (Gk)]`; pupil — the hole that lets light in `[pupilla = little doll (L)]`; optic — relating to the eye or sight `[optikos = of sight (Gk)]`; cochlea — ear part making signals (compared to retina) `[kochlias = snail (Gk)]`; protractor — a tool for measuring angles `[protrahere = to draw out (L)]`

### P6.4 Detecting light and colour
**Tier 2:** detect — sense that light is present `[detegere = to uncover (L)]`; absorb — take in and stop a colour `[ab- = away; sorbere = to suck in (L)]`; reflect — bounce a colour back `[re- = back; flectere = to bend (L)]`; transmit — let a colour pass through `[trans- = across; mittere = to send (L)]`; combine — join colours together `[com- = together; bini = two-by-two (L)]`; perception — how our senses interpret light `[per- = through; capere = to take (L)]`; continuous — an unbroken range `[con- = together; tenere = to hold (L)]`; composed — made up of parts `[com- = together; ponere = to place (L)]`
**Tier 3:** spectrum — the band of visible colours `[spectrum = appearance (L)]`; prism — a triangular block that splits light `[prisma = something sawn (Gk)]`; refract — light bending and splitting `[re- = back; frangere = to break (L)]`; refraction — the bending that splits white light `[re- = back; frangere = to break (L)]`; filter — a material that transmits one colour `[filtrum = felt (L)]`; wavelength — distance between two peaks, sets colour `[wave + length]`; indigo — a blue-violet spectrum colour `[indikon = from India (Gk)]`; violet — the shortest-wavelength spectrum colour `[viola = violet flower (L)]`; retina — the colour-detecting layer of the eye `[rete = net (L)]`; cone — retina cell that detects a colour `[konos = cone (Gk)]`; magenta — a printing primary colour `[—]`; cyan — a printing primary colour `[kyanos = dark blue (Gk)]`

## Physics — Year 3

## P7 Electricity in circuits

### P7.1 Conductors and insulators
**Tier 2:** transfer — moving energy from one place to another `[trans- = across (L); -fer = carry]`; component — a part within a larger circuit `[com- = together (L); -pon = place]`; charge — a property carried by particles `[—]`; property — a characteristic a material has `[—]`; source — where energy comes from `[—]`; conduct — to allow energy/current through `[con- = together (L); -duct = lead]`; microscopic — at the tiny particle scale `[micro- = small (Gk); -scop = look]`; macroscopic — at the large visible scale `[macro- = large (Gk); -scop = look]`
**Tier 3:** conductor — material that lets current through `[con-duct = lead through (L); -or = agent]`; insulator — material that blocks current `[insula = island (L); -or = agent]`; electron — negative subatomic particle `[electro- = amber/electric (Gk); -on]`; delocalised — electrons free to move `[de- = away (L); local = place]`; subatomic — smaller than an atom `[sub- = under (L); atom]`; terminal — the end of a cell (+/−) `[termin = boundary/end (L)]`; coulomb — unit of charge (C) `[—]`; circuit — a closed loop for current `[circu- = around (L); -it = go]`

## P7 Electricity in circuits

### P7.2 Circuits, current, potential difference and resistance
**Tier 2:** current — the flow of charge `[curr- = to run/flow (L)]`; potential — stored capacity to do work `[potent = powerful (L)]`; resistance — opposition to something `[re- = against (L); -sist = stand]`; measure — to find a quantity `[mensura = measurement (L)]`; rate — how fast something happens `[rat- = reckon (L)]`; quantity — a measurable amount `[quant- = how much (L)]`; convert — to change units/form `[con- = with (L); -vert = turn]`; rearrange — to reorder an equation `[re- = again (L); arrange]`; supply — to provide/give `[sub- = up (L); -ply = fill]`; equation — statement that two sides are equal `[equa- = equal (L)]`
**Tier 3:** potential difference — energy per coulomb of charge `[potent = powerful (L)]`; ammeter — instrument measuring current `[amp + -meter = measure (Gk)]`; voltmeter — instrument measuring voltage `[volt + -meter = measure (Gk)]`; amp/ampere — unit of current (A) `[—]`; volt — unit of potential difference (V) `[—]`; ohm — unit of resistance (Ω) `[—]`; resistor — component that opposes current `[re-sist = stand against (L); -or]`; joule — unit of energy (J) `[—]`; cell — single unit of a battery `[cella = small room (L)]`; powerpack — variable power supply `[—]`

## P7 Electricity in circuits

### P7.3 Series and parallel circuits
**Tier 2:** connect — to join together `[con- = together (L); -nect = bind]`; combine — to join into one `[com- = together (L); bini = two (L)]`; independent — not affecting one another `[in- = not (L); depend = hang from]`; additive — able to be added together `[add- = to give to (L)]`; constant — staying the same `[con-stare = stand firm (L)]`; interact — to act on each other `[inter- = between (L); act]`; conservation — keeping a total unchanged `[con- = together (L); serv- = keep]`
**Tier 3:** series — components in one loop `[seri- = in a row (L)]`; parallel — components in separate loops `[para- = beside (Gk); allel- = one another]`; conservation of energy — energy cannot be created/destroyed `[con-serv = keep together (L)]`; loop — a complete circuit path `[—]`

## P8 Static electricity

### P8.1 Static charges
**Tier 2:** charge — property causing electrical force `[—]`; attract — to pull together `[at- = toward (L); -tract = draw]`; repel — to push apart `[re- = back (L); -pel = drive]`; neutral — having no overall charge `[neuter = neither (L)]`; transfer — to move from one to another `[trans- = across (L); -fer = carry]`; interact — to act on each other `[inter- = between (L); act]`; property — a material's characteristic `[—]`; phenomenon — an observable event `[phaino = to appear (Gk)]`
**Tier 3:** static charge — built-up stationary charge `[stat- = standing (Gk/L)]`; static electricity — non-moving accumulated charge `[stat- = standing (Gk/L)]`; positive — charge from losing electrons `[posit = placed (L)]`; negative — charge from gaining electrons `[neg- = deny (L)]`; proton — positive subatomic particle `[proto- = first (Gk); -on]`; electron — negative subatomic particle `[electro- = amber (Gk); -on]`; neutron — neutral subatomic particle `[neut- = neither (L); -on]`; nucleus — centre of an atom `[nucle- = kernel (L)]`; friction — force from surfaces rubbing `[fric- = rub (L)]`; acrylic — a plastic that holds charge `[—]`; polyester — a plastic that gains charge `[poly- = many (Gk); ester]`

## P8 Static electricity

### P8.2 Electric fields
**Tier 2:** field — region where a force acts `[—]`; force — a push or pull `[fort- = strong (L)]`; exert — to apply a force `[ex- = out (L); -sert = put forth]`; attract — to pull together `[at- = toward (L); -tract = draw]`; repel — to push apart `[re- = back (L); -pel = drive]`; strength — how strong something is `[—]`; direction — the way something points `[di- = apart (L); -rect = straight]`; distance — space between two points `[di- = apart (L); -stant = stand]`; illustrate — to show/demonstrate `[in- = on (L); lustr- = light]`
**Tier 3:** electric field — region where charges feel force `[electro- = amber (Gk)]`; electrostatic force — force between static charges `[electro- + stat- = standing (Gk)]`; field lines — lines showing field direction/strength `[—]`; non-contact force — force acting without touching `[non- = not (L); contact = touch]`; electromagnetic — relating to electric and magnetic force `[electro- + magnet]`

## P9 Magnets

### P9.1 Magnets and magnetic fields
**Tier 2:** field — region where a force acts `[—]`; attract — to pull together `[at- = toward (L); -tract = draw]`; repel — to push apart `[re- = back (L); -pel = drive]`; pole — an end of a magnet `[pol- = axis end (Gk)]`; region — an area around something `[reg- = rule/area (L)]`; direction — the way something points `[di- + -rect = straight (L)]`; element — a pure substance of one atom type `[element- = first principle (L)]`; alloy — a mixture of metals `[alligare = to bind (L)]`; combine — to merge together `[com- = together (L); bini = two]`; orientation — the way it is positioned `[orient = rising/east (L)]`
**Tier 3:** magnetic field — region around a magnet where force acts `[magnet- (Gk, from Magnesia)]`; magnetic field lines — lines showing field shape/direction `[magnet-]`; permanent magnet — always-magnetic object `[per- = through (L); man- = remain]`; temporary magnet — magnetic only in a field `[tempor- = time (L)]`; induced magnetism — magnetism caused by a nearby field `[in- = in (L); -duc = lead]`; iron filings — tiny iron pieces showing field `[—]`; bar magnet — rectangular permanent magnet `[—]`; north pole — field-source end of magnet `[—]`; south pole — field-sink end of magnet `[—]`; cobalt — a magnetic element `[—]`; nickel — a magnetic element `[—]`; steel — magnetic iron-carbon alloy `[—]`

## P9 Magnets

### P9.2 Earth's magnetism
**Tier 2:** axis — line something spins around `[axis = axle (L)]`; core — the central part `[cor- = heart (L)]`; sphere — a ball-shaped object `[sphaira = ball (Gk)]`; geographic — relating to the Earth's surface `[geo- = earth (Gk); -graph = write]`; distinguish — to tell apart `[dis- = apart (L); -stingu = mark]`; layer — a level of material `[—]`; solid — a firm state of matter `[solidus = firm (L)]`; liquid — a flowing state of matter `[liqu- = fluid (L)]`
**Tier 3:** compass — device that points to magnetic north `[com- = together (L); pass- = step]`; spindle — pin a compass turns on `[spin]`; magnetic north pole — north end of Earth's field `[magnet-]`; magnetic field of Earth — Earth's surrounding magnetic region `[magnet-]`; inner core — solid central layer of Earth `[—]`; outer core — liquid layer of Earth `[—]`; geographic North Pole — top point of Earth's axis `[geo- = earth (Gk)]`

## P9 Magnets

### P9.3 Electromagnets and motors
**Tier 2:** generate — to produce/create `[gener- = beget (L)]`; coil — wire wound in loops `[colligere = gather (L)]`; strength — how strong something is `[—]`; increase — to make larger `[in- = in (L); -cresc = grow]`; decrease — to make smaller `[de- = down (L); -cresc = grow]`; precise — closely grouped/exact `[prae- = before (L); -cis = cut]`; variable — a factor that can change `[vari- = change (L)]`; investigate — to examine systematically `[in- + vestig- = track (L)]`; encircle — to surround `[en- = in (L); circ- = ring]`; manipulate — to change/alter `[manus = hand (L)]`
**Tier 3:** electromagnet — magnet made by a current `[electro- = electric (Gk); magnet]`; solenoid — coil of wire producing a field `[solen- = pipe/channel (Gk); -oid = like]`; iron core — magnetic centre of an electromagnet `[—]`; motor — device turning current into motion `[mot- = move (L); -or]`; magnetic core — magnetisable centre of a coil `[magnet-]`; direct current (DC) — current flowing one way `[—]`; precision — closeness of repeated measurements `[prae-cis = cut before (L)]`; independent variable — the factor deliberately changed `[in- = not (L); depend]`; dependent variable — the factor measured `[depend = hang from (L)]`

## P10 The particle model (physics)

### P10.1 Particle model and density
**Tier 2:** particle — a tiny piece of matter `[part- = part (L); -icle = small]`; density — mass per unit volume `[dens- = thick (L)]`; volume — amount of space occupied `[volumen = roll/size (L)]`; mass — amount of matter in an object `[massa = lump (L)]`; matter — substance made of particles `[materia = material (L)]`; substance — a specific kind of matter `[sub- = under (L); -stance = stand]`; arrangement — how things are ordered `[—]`; property — a characteristic of matter `[—]`; float — to rest on a fluid `[—]`; equation — a mathematical statement of equality `[equa- = equal (L)]`; reproduce — to get the same result again `[re- = again (L); pro-duc = bring forth]`
**Tier 3:** particle model — theory that matter is made of particles `[part- = part (L)]`; solid — fixed-shape state of matter `[solidus = firm (L)]`; liquid — flowing state of matter `[liqu- = fluid (L)]`; gas — freely-spreading state of matter `[—]`; states of matter — solid, liquid, gas forms `[stat- = standing (L)]`; rho — Greek symbol (ρ) for density `[rho (Gk letter)]`; meniscus — curved surface of a liquid `[mene = crescent moon (Gk)]`; Brownian motion — random movement of particles `[—]`; diffusion — spreading of particles `[dif- = apart (L); -fus = pour]`; repeatable — same experimenter, similar results `[re- = again (L); pet- = seek]`; reproducible — different people, similar results `[re- = again (L); pro-duc = bring forth]`; measuring cylinder — vessel for measuring volume `[—]`

## P10 The particle model (physics)

### P10.2 Energy in matter
**Tier 2:** energy — capacity to do work `[en- = in (Gk); ergon = work]`; transfer — to move energy `[trans- = across (L); -fer = carry]`; supply — to provide `[sub- = up (L); -ply = fill]`; absorb — to take in `[ab- = from (L); -sorb = suck]`; vibrate — to move rapidly to and fro `[vibr- = shake (L)]`; internal — on the inside `[intern- = inward (L)]`; temperature — measure of hotness `[tempera- = mix/regulate (L)]`; increase — to grow larger `[in- + -cresc = grow (L)]`
**Tier 3:** internal energy — total energy of particles `[intern- = inward (L)]`; kinetic energy store — energy of moving particles `[kine- = move (Gk)]`; change of state — solid↔liquid↔gas transition `[stat- = standing (L)]`; melting point — temperature solid becomes liquid `[—]`; boiling point — temperature liquid becomes gas `[—]`; evaporation — liquid turning to gas `[e- = out (L); vapor = steam]`; condensation — gas turning to liquid `[con- = together (L); dens- = thick]`; solidification — liquid turning to solid `[solidus = firm (L)]`; heating curve — graph of temperature over time `[—]`; cooling curve — graph of temperature falling `[—]`

## P11 Space

### P11.1 Stars, planets and galaxies
**Tier 2:** orbit — path of one object around another `[orbita = track (L)]`; sphere — a ball-shaped object `[sphaira = ball (Gk)]`; spherical — shaped like a sphere `[sphaira = ball (Gk)]`; scale — relative size or ratio `[scala = ladder (L)]`; collection — a group gathered together `[col- = together (L); -lect = gather]`; distance — space between objects `[di- = apart (L); -stant = stand]`; gravitational — relating to gravity `[grav- = heavy (L)]`
**Tier 3:** solar system — Sun and its orbiting bodies `[sol- = sun (L)]`; galaxy — a vast collection of stars `[gala- = milk (Gk)]`; Milky Way — our spiral galaxy `[—]`; spiral galaxy — galaxy with spiral arms `[spir- = coil (L)]`; nebula/nebulae — cloud of dust and gas `[nebula = mist (L)]`; elliptical — squashed-circle shaped `[ellipsis = falling short (Gk)]`; astronomical — relating to space objects `[astro- = star (Gk); -nom = law]`; dwarf planet — small non-dominant orbiting body `[—]`; asteroid — small rocky orbiting body `[aster- = star (Gk); -oid = like]`; comet — icy body orbiting the Sun `[comet- = long-haired (Gk)]`; light year — distance light travels in a year `[—]`; cosmology — study of the universe `[cosmo- = universe (Gk); -logy = study]`; satellite — object orbiting a planet `[satelles = attendant (L)]`; universe — all of space and matter `[uni- = one (L); vers- = turn]`

## P11 Space

### P11.2 The seasons
**Tier 2:** axis — line the Earth spins around `[axis = axle (L)]`; tilt — a slope from vertical `[—]`; orbit — path around the Sun `[orbita = track (L)]`; direct — hitting head-on `[di- = apart (L); -rect = straight]`; parallel — running in the same direction `[para- = beside (Gk); allel-]`; perpendicular — at right angles `[per- = through (L); pend- = hang]`; transverse — vibrating across the direction of travel `[trans- = across (L); -vers = turn]`; vertical — straight up and down `[vertic- = highest point (L)]`; illuminate — to light up `[in- = on (L); lumin- = light]`; simultaneous — happening at the same time `[simul = at once (L)]`
**Tier 3:** hemisphere — half of a sphere `[hemi- = half (Gk); sphaira = ball]`; equator — line around the Earth's middle `[aequa- = equal (L)]`; northern hemisphere — Earth's upper half `[hemi- = half (Gk)]`; southern hemisphere — Earth's lower half `[hemi- = half (Gk)]`; glancing — light hitting at a shallow angle `[—]`; normal line — line at right angles to a surface `[norma = carpenter's square (L)]`; sunrise — Sun appearing at dawn `[—]`; sunset — Sun disappearing at dusk `[—]`; vacuum — space with no particles `[vacu- = empty (L)]`

## P11 Space

### P11.3 Weight
**Tier 2:** weight — force of gravity on an object `[—]`; mass — amount of matter in an object `[massa = lump (L)]`; force — a push or pull `[fort- = strong (L)]`; gravity — pull toward a planet `[grav- = heavy (L)]`; gravitational — relating to gravity `[grav- = heavy (L)]`; matter — substance made of particles `[materia = material (L)]`; exert — to apply a force `[ex- = out (L); -sert = put forth]`; convert — to change units `[con- = with (L); -vert = turn]`; accurate — close to the true value `[ac- = to (L); cura = care]`; calibrate — to mark a scale accurately `[calibre = bore size]`; infer — to conclude from evidence `[in- = in (L); -fer = carry]`
**Tier 3:** newton — unit of force (N) `[—]`; kilogram — unit of mass (kg) `[kilo- = thousand (Gk); gram]`; gravitational field strength — force per kg of mass `[grav- = heavy (L)]`; gravitational field — region where gravity acts `[grav- = heavy (L)]`; weightless — having no weight `[—]`; newtonmeter — device measuring force `[newton + -meter = measure (Gk)]`; extension — amount a spring stretches `[ex- = out (L); -tens = stretch]`; calibration curve — graph relating readings to values `[—]`; accuracy — closeness to the true value `[cura = care (L)]`; true value — the correct target measurement `[—]`
